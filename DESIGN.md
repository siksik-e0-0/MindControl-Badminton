# 배드민턴 복식 시뮬레이터 — 설계 문서

이 문서는 대화로 확정된 것과, 원본 조사 자료(제안 단계)를 구분해서 기록한다.
"확정"이라고 적힌 것만 사용자가 실제로 승인한 결정이고, "제안"은 아직 이의가 없어서 채택 중인 초안이다.

## 목적

실전 배드민턴 복식 승률 향상을 위한 시뮬레이션/교육 게임. 특히 초심 유저에게 로테이션·코스·기술 선택을 가르치는 것이 핵심 가치.

## 확정된 설계

### 코트 규격 (BWF 공식, 복식)

- 전체 13.4m x 6.1m
- 네트 높이: 중앙 1.524m, 포스트 1.55m
- 숏서브라인: 네트에서 1.98m
- 롱서브라인(복식): 백바운더리 안쪽 0.76m
- 탑뷰 렌더링 비율 2.2:1(13.4:6.1) 고정

### 화면 흐름

```
타이틀 → 모드선택(남복/여복/혼복) → 급수선택(초심/C/B/A)
→ [서브 단계] → 코트 화면(턴제 랠리)
→ [랠리 종료, "상세보기" 클릭시만 리포트 노출]
→ 매치 종료 종합 리포트
```

### 코스 선택 (코트 터치 방식)

기술카드 탭 → 도달가능영역 하이라이트 → 코트 터치로 마커 표시(미확정)
→ "확정" 버튼 누르기 전까지 자유 재조정 가능 (되돌리기 범위 = 이 구간만, A안)
→ 확정 후 궤적 애니메이션 → 상대 AI 턴.
확정 버튼을 누른 뒤에는 되돌리기 없음.

트레이드오프로 검토했던 대안(카드 옆 서브메뉴 방식)은 채택 안 함 — 터치 방식은 연속 좌표라 물리엔진 입력과 바로 연결되고, 터치 정밀도 문제는 확정 전 되돌리기(A안)로 커버하기로 함.

### 로테이션 화살표

모드별 포메이션 상태머신에서 로테이션 필요 시 화살표 오버레이.
- 혼복: 여성전위·남성후위 고정 + 특정 트리거(남성에게 짧은 헤어핀 반구 등)에서만 임시 로테이션
- 남복: 매 랠리 공수전환 신호(로브/스매시/헤어핀 등)마다 로테이션

색상: 노랑=권장, 빨강=시급.
**초급만 텍스트 툴팁 동반** (예: "상대 스매시 대비 사이드바이사이드 전환 필요") — 이건 검증된 효과가 아니라 "교육 목적에 도움될 것"이라는 가설. C조 이상은 화살표만, 툴팁 없음.

### 기술카드

- 급수별 해금: 초심(클리어/드롭/헤어핀) → C조(+스매시/드라이브) → B조 이상(+디셉션/크로스드롭/백핸드클리어 등)
- 배열 순서: **난이도순(=해금순) 고정, 위치 이동 없음.** 새 기술은 항상 뒤에 추가된다.
- 미해금 카드 UI: 채도 0 · 명도 -40% · 자물쇠 아이콘 · "O조 해금" 라벨 · 점선 테두리 · 탭 무반응(툴팁으로 해금조건만 표시)
- 추천 카드(현재 상황 추천 기술): **제자리에서 테두리/라벨만 강조, 위치 이동 없음.** 초급은 강하게, C조 이상은 약하게 또는 없음(강도는 미확정).
  - 주의: 이전에 "맨 앞으로 이동"으로 논의됐다가 "카드 위치 고정" 원칙과 충돌해서 이 방식으로 정정됨.

### 서브 단계 (별도 턴)

랠리 시작마다 코트 화면 진입 전에 독립된 단계로 처리한다.
- 코트 터치 영역 = 대각선 상대 서비스 코트로 제한(숏서브라인~롱서브라인, 센터라인 기준)
- 서버 위치(좌/우 서비스코트): **BWF 공식 규칙** — 서버 자기 팀 점수 짝수면 우측, 홀수면 좌측. UI 선택 없이 점수로 자동 결정.
- 서브 종류 및 급수별 해금: 숏서브(초심~), 롱서브(C조~), 플릭서브(B조~, 디셉션류)
- 혼복=남성이 숏서브라인보다 물러나서 서브 / 남복=네트 바짝 앞에서 서브 — **이건 전술 관행이지 BWF 공식 규칙이 아님.** 원본 조사 자료가 설명한 포메이션 관습을 게임 데이터로 반영하는 것.

### 랠리 종료 리포트

전 급수 공통으로 **"상세보기" 클릭 시에만 노출**. 자동 팝업 없음(급수 무관, 초급 예외도 기각됨).
내용: 해당 랠리의 여러 턴(전체 또는 최근 N턴) 리플레이 + 그 중 임의 턴(마지막 턴에 한정 아님)에서 다른 선택을 했을 경우의 몬테카를로 기반 대안 승률 비교.
예시(확정 와이어프레임): 3턴짜리 랠리를 턴1~턴3까지 전부 보여주고, "턴2에서 스트레이트 드롭이었다면 승률 +8%"처럼 중간 턴에 대해서도 대안을 제시함.

### 매치 종료 종합 리포트

- 기술 사용 빈도 차트
- 코스별 성공률(스트레이트 vs 크로스)
- 로테이션 타이밍: **등급제(상/중/하)**, 숫자 점수 아님
- 놓친 랠리 TOP3 + 대안 승률 비교

## 미확정 항목 (다음에 결정할 것)

- 추천 카드 강조가 상급자에서 완전히 꺼지는지, 약하게라도 유지되는지
- 모드/급수 선택 화면 자체의 와이어프레임 (구현은 최소 버튼 구성으로 했고, 디자인은 미확정)
- 기술/서브 파라미터 실제 수치(궤적 높이, 실수 확률, 압박도 등)
  - 물리엔진 RK4 검증 결과(2026-09-22, `web/scripts/verify-trajectory.ts` 실행): 항력계수는 셔틀콕 종단속도 약 6.7~6.8m/s 역산값(k≈0.21)으로 재보정함. 헤어핀/드롭/스매시는 코트 규격(13.4m) 내에 들어오는 사거리가 나왔지만, 클리어는 현재 파라미터(속도22m/s·발사각75°)로 수평거리 3.57m밖에 안 나와 실제 클리어(코트 깊숙이 보내는 샷) 용도에 안 맞음. 발사각/속도 재조정 필요 — 아직 안 함.

## 기술스택 (제안 — 원본 조사 자료 권장안, 대화로 별도 확정된 적 없음)

이견 없으면 이대로 채택하되, 이의 나오면 바로 변경.

- 프론트: Phaser 3(코트 렌더링) + React(메뉴/설정/리포트 화면) + TypeScript
- 물리: 2차 항력(quadratic drag) 모델, 항력계수 Cd ≈ 0.5, 수치적분(오일러 또는 RK4), 60fps 프레임 단위 계산
- 백엔드: Node.js/TS(Express 또는 Fastify, 실시간 게임 로직) + Python(몬테카를로/MCTS 시뮬레이터), REST 또는 메시지 큐로 연결
- DB: 초기 SQLite → 데이터/유저 증가 시 PostgreSQL
- 상대 AI: 위치×급수×직전샷 확률표(JSON)로 시작 → 이후 마르코프 체인/MCTS로 고도화

## MVP 로드맵

### Phase 1 (지금 진행)

- 모드/급수 선택 화면
- 서브 단계 + 기술카드 4종(클리어/드롭/스매시/헤어핀) 턴제 랠리
- 규칙기반 확률 AI (JSON 확률표)
- 물리 시뮬레이션 단순화 버전(2차 항력 근사)
- 로컬 상태 기반 진행 (DB 연동 없음)

### Phase 2

- 데이터 로깅 파이프라인 연결
- 기술 세트 급수별 확장 (디셉션, 크로스드롭 등)

### Phase 3

- Python 기반 MCTS 시뮬레이터
- "대안 선택 시 승률 비교" 리포트 기능 (랠리 리포트/매치 리포트에 실제 수치 반영)

## Phase1 구현에서 임의로 둔 단순화 (사용자 확인 필요 — 확정 아님)

구현하면서 설계 문서에 없던 결정이 필요했던 지점. 전부 바꿀 수 있는 임시값이다.

- **플레이어가 항상 먼저 서브** (토스 없음).
- **로테이션 실행 = 플레이어 버튼** (확정 설계대로: 화살표는 *실행할 타이밍* 안내). 플레이어 턴에 "로테이션 → ○○" 버튼으로 톱앤백↔사이드바이사이드를 바꾸고, 확정 전까지 몇 번이든 가능. 화살표는 *현재 포메이션이 전술 상황이 요구하는 것과 다를 때만* 뜨고 제대로 돌리면 사라진다. 혼복은 고정형이라 임시 로테이션 뒤 "기본형 복귀 권장" 신호. 상대 AI 팀은 항상 제때 자동 로테이션한다고 가정.
  - ※ 2026-09-22 첫 구현은 로테이션을 자동으로 하고 "로테이션 타이밍" 등급을 *신호 대비 기술 선택 일치율*로 대체했었음. 합의 없는 설계 변경이라 리뷰어 지적으로 걷어냄(2026-09-23).
- **로테이션 타이밍 등급 산식(임시값, 사용자 확인 필요)**: 신호가 뜬 상황(에피소드)마다 그 턴에 바로 돌리면 1점, 같은 랠리 안에서 늦게 돌리면 0.5점, 끝내 안 돌리면 0점. 평균 0.7↑ 상 / 0.4↑ 중 / 미만 하. 신호 없이 한 로테이션은 등급에 안 넣고 횟수만 리포트에 표시.
- **수비 전환 신호 트리거에 드롭 포함**: 상대 스매시뿐 아니라 드롭도 수비(사이드바이사이드) 신호로 취급. 근거는 "드롭에 네트로 끌려나가면 올려주게 되고, 올린 뒤엔 수비 대형"이라는 단순화. 초심 AI는 스매시가 없어서 이게 없으면 초심에서 수비 신호가 한 번도 안 뜬다.
- **실수/위너 판정 모델**: 치는 쪽 실수 확률 = 기술 `riskOfError` × (AI면 급수 계수 초심1.6/C1.0/B0.7/A0.5, 플레이어면 1.0) × **경계 계수**. 받는 쪽이 못 받을(위너) 확률 = 기술 `pressure` × 급수별 리시브 미스 계수(초심0.6/C0.4/B0.25/A0.15) × **빈 곳 계수**. 전부 튜닝 전 임시값.
  - 경계 계수: 낙하 목표가 영역 경계(사이드/베이스라인/네트, 서브는 서비스박스 경계)에서 0.6m 안쪽으로 들어오면 선형으로 커져 라인 위에서 2배. 이 경우 실수는 "아웃"으로 기록.
  - 빈 곳 계수: 가장 가까운 상대 선수까지 거리 ÷ 2m 를 0.4~1.6으로 자른 값. 선수 바로 앞이면 0.4배, 빈 곳이면 최대 1.6배. 그래서 포메이션이 역학적으로 의미를 가진다 — 톱앤백은 네트 앞을 잘 막고 사이드가 비고, 사이드바이사이드는 그 반대.
  - 받는 선수 = 낙하점에 가장 가까운 선수. 그 역할(톱앤백은 전위/후위, 사이드바이사이드는 낙하 깊이로)이 AI 기술 선택 확률표의 입력이 된다.
  - ※ 2026-09-22 첫 구현은 목표 좌표가 판정에 전혀 안 들어가 코스 선택이 전술이 아니었음(리뷰어 지적으로 2026-09-23 반영).
  - 아직 안 넣은 것: 치는 쪽 자기 포메이션이 자기 샷 품질에 미치는 영향(예: 사이드바이사이드에서 스매시는 약함), 선수 이동 속도/도달 여부, 셔틀 높이.
- **AI 목표 지점**: 기술 성격별 깊이 구간(클리어/로브=깊게, 스매시=중간, 드롭/헤어핀=네트 앞) 안에서 후보 K개를 뽑아 플레이어 선수들에게서 가장 먼 지점(K: 초심1=무작위, C2, B3, A4). 상급 AI일수록 빈 곳을 노린다는 단순화.
- **서브를 기술 신호로 환산**: 롱/플릭 서브=클리어처럼(공격 기회), 숏서브=헤어핀처럼 취급.
- **혼복 예외 트리거**: "남성에게 짧은 헤어핀 반구"를 *상대가 헤어핀을 침*으로 단순화(남녀 위치를 아직 모델링 안 함).
- **추천 기술 규칙**: 수비 신호→클리어, 공격 신호→스매시(해금 시) 아니면 드롭, 상대 헤어핀→헤어핀. 설명 가능한 단순 규칙이지 전술 모델 아님. 헤드리스 시뮬레이션에서는 이 규칙 때문에 헤어핀 왕복이 잦게 나옴(품질 개선 여지).
- **대안 승률 비교(몬테카를로)**: Phase3 범위. 리포트에 가짜 수치를 넣지 않고 "미제공"으로 표기.
- **모드/급수 선택 화면**: 와이어프레임을 따로 잡지 않았으므로 버튼 두 줄의 최소 구성.
- **매치 단위**: 1게임(21점, 2점차, 30점 상한). 세트(3게임) 개념은 아직 없음.

## 검증 기록 (2026-09-22 ~ 23)

- `web/scripts/verify-course.ts` (2026-09-23): 경계 계수 중앙1.0/라인근처1.67/라인위2.0/네트앞1.75. 빈 곳 계수 — 톱앤백 상대: 전위 앞 0.40, 사이드 1.54, 네트 앞 0.60 / 사이드바이사이드 상대: 사이드 0.65, 네트 앞 1.60 (포메이션별 강약이 뒤집히는 것 확인). AI 조준 K=4가 K=1보다 평균 빈 거리 2.41m vs 1.68m. 시드 몬테카를로 20000회(C조 스매시, 상대 톱앤백): 선수 앞 위너율 8.0% vs 빈 사이드 27.1%; 라인 5cm 안쪽 조준은 아웃 56.9%(중앙/안쪽은 아웃 0%). 전부 통과.
- `web/scripts/verify-match.ts` (2026-09-23 재실행): 12매치 완주·정합성 통과. 로테이션 타이밍 등급 — 신호를 전부 즉시 따르는 봇은 상(즉시=에피소드 수), 전부 무시하는 봇은 하(놓침=에피소드 수), 3개 조합(남복C/남복초심/혼복B) 모두 확인. AI가 빈 곳을 노리게 되면서 난이도가 올라감(무작위 조준 봇 기준 플레이어 승 4/12, 초심 3매치는 전부 플레이어 승).

- `web/scripts/verify-match.ts`: 순수 리듀서(`src/game/match.ts`)로 모드3×급수4 = 12매치를 헤드리스로 완주. 전부 21점 종료, 점수 합=랠리 수, 랠리 승자=마지막 턴 결과, 서브 좌우=자기 팀 점수 홀짝 규칙 일치, 듀스 처리 확인. 초심은 플레이어 승, 상급은 AI 승 쪽으로 기울어 난이도 곡선이 의도대로 나옴.
- `web/scripts/verify-ai.ts`: 확률표 샘플링 20000회, 기대치와 1%p 이내.
- `web/scripts/verify-trajectory.ts`, `verify-mapping.ts`: RK4 궤적과 화면 매핑(감속 곡선 유지) 확인.
- `web/scripts/screenshot.mjs`: 실제 chromium으로 설정→서브→랠리→로테이션 버튼→리포트 화면 스크린샷.

## 하지 말아야 할 것

- 사용자가 명시적으로 확정하지 않은 것을 확정된 것처럼 서술하지 않는다.
- BWF 공식 규칙과 전술 관행(포메이션 관습)을 같은 근거 급으로 섞어 쓰지 않는다.
- 되돌리기/UI 동작 범위를 임의로 넓히거나 좁히지 않는다 — A안(확정 전 마커 조정만)으로 고정.

## 방향 전환 (2026-09-23): 상황(시나리오) 학습 뷰어

사용자 지시: "복식을 배우기 위한 교육 시뮬레이터. 상황 데이터를 많이 만들어서 보여주며 학습. 로테이션·코스·공수전환. 가볍게, 핸드폰에서 바로 실행." 이에 따라 `web/`(Vite+React+Phaser 매치 게임)은 참고용으로만 두고, 제품은 `mobile/index.html` 단일 파일(바닐라 JS + SVG, 빌드·외부 의존 없음)로 바꿨다.

### 시나리오 데이터 형식 (v0.3, `mobile/index.html` 안 `DATA`)

```
DATA.topics[]        주제 (예: 숏서브 · 서브 측 — 종목은 sport 필드, 제목에 종목 접두 없음: v1.2.1)
  id, title
  us: ["S","P"]      우리 팀 선수 id (화면 아래, y>0)
  them: ["R","RP"]   상대 팀 선수 id (화면 위, y<0)
  initial: {id:[x,y]} 시작 위치 (m, BWF 코트: x ±3.05, y ±6.7)
  start: 노드 id
  nodes: { id: 노드 }

노드
  title              "3구: 스매시" 처럼 몇 구째인지 + 무엇인지
  shot?              { by, type, from:[x,y], to:[x,y], height: low|mid|high, speed: fast|slow }
  pos                샷 뒤 네 명의 위치 {id:[x,y]}
  formation          { us: "톱앤백(공격)", them: "사이드바이사이드(수비)" }
  arrow?             { side: us|them, axis: "↔"|"↕", urgency: 권장|시급, text }  로테이션 신호
  notes[]            { kind, text, src:[출처 키...] }
                     kind = 기본 / 조건 / 위험 / 대형 / 코스 / 로테이션 / 상황 / 목적
  prompt? branches?  갈래 질문과 선택지 [{label, to}]
  next?              다음 노드 id (갈래 없을 때)
  end?               갈래 끝 안내문

SOURCES{key}         { label, url?, note }  출처는 여기 한 번만 두고 줄에서는 키로 참조
  "브"=사용자 브리프, "확"=사용자 확인, "미"=미검증(내 기억), 나머지=공개 코칭 자료 URL
```

- 별도 `.json` 파일로 분리하지 않는다: 아이폰에서 파일을 직접 열 때(`file://`) `fetch`가 막혀 단일 파일이어야 "바로 실행"이 된다. 데이터는 `<script>` 안 객체로 둔다.
- 설명 한 줄마다 출처 키가 붙고, 배지를 누르면 원문이 열린다. 출처 요약(note)은 그 자료가 실제로 말하는 내용만 적는다. 원문보다 강한 단정("반드시", "~만")은 쓰지 않는다 — 2026-09-23 리뷰어 지적(오역 '두세 발', 강한 단정 '아주 좋을 때만')으로 17쪽 원문 문단을 다시 받아 전 줄 대조했다.
- 무결성 검사: `cd web && node scripts/check-data.mjs` — `vm`으로 `DATA`를 평가해 미정의 출처 키, 끊긴 갈래(`to`/`next`), 도달 불가 노드, `initial`/`pos`에 없는 선수 id, 미검증 태그 수를 센다(v0.5: 주제 8·노드 124·노트 277·미검증 0·문제 0).
- 확인 방법: `cd web && node scripts/shot-mobile.mjs /tmp/mshots`(숏서브 2주제), `node scripts/shot-flick.mjs /tmp/fshots`(롱 서브 2주제), `node scripts/shot-mixed.mjs /tmp/xshots`(혼복), `node scripts/shot-v05.mjs /tmp/v05shots`(초구 코스·드라이브 랠리·공수 전환), `node scripts/contact-sheet.mjs <in> <out> 4`.

### 주제 현황 (v0.5, 2026-09-23)
- 남복 · 숏서브 · 서브 측 (35노드) / 리시브 측 (33노드). 초구 응수는 2단계: 기술(푸시/드라이브/네트샷/리프트) → 코스.
  - 푸시: 스트레이트 트램라인(기본) / 크로스 트램라인(변화) / 서버 몸(서버가 앞에 남았을 때) / 뒤 코너(일찍 잡았고 후위가 앞으로 기울었을 때).
  - 드라이브: 서버 직접 / 후위 쪽(전위가 네트에 바짝 붙었을 때).
  - 네트샷: 스트레이트 / 중앙(T 앞) / 크로스(트램 쪽으로 넓게 유도할 때).
  - 코스 이름은 리시버 기준(스트레이트 = 자기 사이드)이고 갈래 라벨에 화면 좌우를 병기한다. 출처(badmintonbible 공격각)가 "pushes are usually played straight down the side tramlines", "net shots … straight or to the centre"라고 하므로 스트레이트를 기본 갈래로 뒀다. v0.4까지의 푸시 갈래는 크로스 방향이었다 — 재대조로 정정.
- 남복 · 롱 서브 · 서브 측 (7노드) / 리시브 측 (6노드) — 주제 이름은 v0.8.2에서 "플릭(롱) 서브"→"롱 서브"(사용자 지시). v0.8.3에서 화면 제목·설명·코트 라벨·출처 배지의 "플릭"도 전부 "롱 서브"로 통일.
- 남복 · 드라이브 랠리 (14노드): 평평한 교환 → 누가 잡나(전위 높이 / 머리 위 → 후위 / 네트 아래) → 드라이브·푸시·블록 선택(높이가 정한다) → 상대가 띄우면 내려침 / 올리면 앞뒤 공격 / 우리가 올리면 즉시 좌우. 선수 id F/B(우리 전위·후위), OF/OB(상대).
- 남복 · 공수 전환 랠리 (14노드): 우리 스매시 → 상대 블록(전위 사냥) / 드라이브 카운터(후위가 잡음 → 맞받기 또는 리프트 → 좌우 전환 ↔) → 상대 스매시 → 블록(따라 들어가 앞뒤 재구성 ↕) / 드라이브 / 푸시 / 리프트. 선수 id F/B, O1/O2.
- 혼복 · 숏서브 · 서브 측 (6노드) / 혼복 · 숏/롱 서브 · 리시브 측 (9노드) — v0.4 그대로. 사용자: 현재로 만족, 향후 업데이트. 지금은 남복에 집중.
- 규칙(BWF 9.5, 11.1.4, 13.2.3, 13.3.9)은 `kind: "규칙"` + 출처 `bwf_laws`로 전술 관행과 구분한다. 서브는 리시버만 칠 수 있으므로 "롱 서브를 파트너가 대신 받는" 시나리오는 없다 — 리시버가 뒤로 가서 치고, 역할 교대는 그 뒤에 온다.
- 예정: 좌표 미세 조정, 리프트 코스 갈래(뒤 코너 좌/우). 판단(퀴즈) 모드는 사용자 승인 후 별도.

### v0.6 (2026-09-23): 시작 화면 · 급수 · 갈래 미리보기
- 시작 화면: 종목(남복/혼복) → 급수(D조(초급)/C조/B조/A조) → 시작. 탭은 종목별로 필터(`topic.sport` = `level`|`mixed`). 헤더 오른쪽 버튼(예: "남복 · B조 ▾")으로 다시 연다. 설정은 메모리에만(아이폰 `file://`에서 localStorage가 불확실).
- 갈래 선택 화면: 갈래마다 색을 배정해 코트에 다음 샷을 미리 그린다(버튼의 색 칩과 짝). 갈래가 코스 선택 노드로 이어지면 그 하위 갈래의 첫 샷을 전부 얇게 그리고 대표(첫 갈래) 하나만 굵게. 라벨은 비율 %만.
- 비율(%)은 **추정**이다. 실측 데이터가 없으므로 갈래마다 `w`(4=출처가 '보통/기본'이라 한 것, 3=유효한 주 선택, 2=상황 선택, 1=변화용·드문 것)와 `skill`(+1 상급일수록 흔함: 일찍 잡기·카운터 / -1 초급일수록 흔함: 리프트·미스·늦음 / 0 급수 무관)을 두고, 급수 배수 `SKILL_MULT`(+1: D 0.5, C 0.8, B 1.2, A 1.5 / -1은 반대)로 곱해 정규화한다. 반올림 합이 100이 되게 나머지가 큰 순으로 보정. UI에 "비율은 추정(급수)"이라고 적는다. 규칙으로 정해지는 갈래(누가 서브/리시브하나)는 `noProb: true`로 비율을 생략한다.
- 예(초구 리시버 응수): D조 푸시 15 / 드라이브 10 / 네트샷 30 / 리프트 30 / 미스 15 → A조 38 / 25 / 25 / 8 / 4. 클럽 경험과 다르면 `w`·`skill`·`SKILL_MULT`만 고친다.
- 확인: `node scripts/shot-v06.mjs /tmp/v06shots`(시작 화면 → B조 → D조/A조 전환 비율 → 혼복 noProb). 기존 스크립트 4개는 시작 화면을 지나가도록 고침. `check-data.mjs`는 sport·w·skill·noProb도 검사.

### v0.7 (2026-09-23): 3구 기술→코스 갈래 (서브 측·리시브 측)
- 사용자: "3구의 경우도 2구와 동일하게 추가해야해". 초구 변형마다 3구를 '기술 선택 → 코스 선택 → 샷' 2단계 갈래로 붙였다. 서브 측은 우리(S 또는 P)의 3구, 리시브 측은 상대(S 또는 P)의 3구(사용자가 상대 응수를 고른다). 비율 숫자는 사용자 결정대로 추후 일괄 조정, 화살표/라벨 표시는 v0.6 그대로.
- 구현: 노드를 손으로 쓰지 않고 데이터 구간 끝(`SKILL_MULT` 뒤, `// 뷰어` 앞)의 생성기 `buildThird(topic, specs)`가 파일 로드 때 노드를 만든다. `THIRD.techs`(드라이브/푸시/블록/네트샷/리프트/킬/스매시/드롭/클리어 — 라벨·설명·출처·w/skill·높이·속도·가능 코스)와 `THIRD.courses`(코스 21종 — 라벨·설명·출처·w/skill·목표점)가 단일 출처. spec에는 상황만 적는다: 초구 노드 id(after), 히터, 타점(at), 네 명 위치(pos), 기술군(techs), 상황 노트, 4구 연결(follow: "기술.코스" → next 또는 branches), 대체할 옛 노드(drop).
- 좌표 규칙: 히터 기준 스트레이트 = 같은 x 부호. 목표점 — 트램 [±2.1, 2.6], 네트 [±0.9 | 0 | ∓1.9, 0.8~0.9], 뒤 코너 [±2.2, 5.8], 킬 [±1.8, 2.6], 스매시 [±2.0, 3.8] / [0.1, 3.9] / [1.4, 4.0] / 크로스, 드롭 [1.6, 1.3] / [0, 1.4] / 크로스, 몸·후위·틈은 상대 위치에서 계산. 잎의 대형: 리프트·클리어는 올린 팀 좌우(전위 자기 레인으로 뒤로, 후위 반대 절반) + ↔(우리 시급 / 상대 권장), 상대 후위는 코너로; 스매시·드롭은 파트너가 네트; 후위가 블록/네트샷을 치면 ↕ 교대(bh_rot '셔틀을 따라간다'); 그 외는 가까운 상대가 목표 쪽으로.
- 4구 연결: D1 스매시 센터 → D3a/b/c(기존), r1 드라이브 몸 → r1a2, r1 블록 스트레이트/중앙 → r1b2, r3 리프트 스트레이트 → r3b2, r4 스매시 스트레이트 → r4c1~3(기존). 그 외 잎은 "3구 갈래 끝". 옛 단일 3구 노드(A2, A2x, A2b, A2c, B2, B2p, C2s, C2a, C2b, C2x, D2, r1a, r1b, r1x2, r1body2, r1corner2, r2p2, r3s2, r3a, r3b, r3x2, r4b)는 생성 트리로 대체(삭제). 그 노트 내용은 spec 상황 노트와 템플릿으로 옮겼다.
- 출처: 새 줄은 전부 기존 검증 키(sl_drive, bb_atk_shots, bb_atk_angles, bh_place, bi_net, bh_ros, bb_pos, sl_foot, bh_rot, bh_a2d, bh_front, bb_def_shots, bb_def_angles)의 표현 범위 안에서 썼다. 킬 코스만 badmintonbible 공격각 원문을 다시 받아 확인 — 가파른 킬은 코스 무관, 조금 평평하면 스트레이트(비거리·가로채기·네트 응수 커버), 손이 닿는 한계면 크로스뿐 → 킬 코스는 스트레이트(기본)/크로스 2개. `SOURCES.bb_atk_angles` 요약에 반영.
- 규모: 주제 8, 노드 124 → 378(서브 측 162, 리시브 측 160), 노트 931, 미검증 0, check-data 문제 0. 기술 가중치 기본값(추정): 스매시 4 / 푸시·킬 3 / 드라이브·블록·네트샷·리프트·드롭 2 / 클리어 1; skill 드라이브·블록·킬 +1, 리프트·클리어 −1, 나머지 0. 코스 가중치는 템플릿에.
- 확인: `cd web && node scripts/shot-v07.mjs /tmp/v07shots`(20장, page errors none) → `node scripts/contact-sheet.mjs /tmp/v07shots /tmp/v07sheet 4`. 생성 결과 조회: `node scripts/dump-nodes.mjs serve-side A1_3 A1_3_push A1_3_push_p_straight`.
- 알려진 한계: 같은 지점에서 나란히 나가는 샷(스매시 4코스 등)의 % 라벨이 겹친다(표시 방식은 사용자 승인대로 유지). 옛 스크립트 shot-mobile/shot-v05의 D2·r4b 경로는 트리가 바뀌어 더 이상 맞지 않는다(shot-v07이 대체). 3구 잎의 상대 이동은 '가까운 쪽이 목표로 0.5~0.6' 규칙이라 초안 수준.
- 리뷰어 지적 반영(2026-09-23): 리시브 측 "상대 블록" 갈래는 생성기가 상대 P를 전위·S를 후위로 교대시키는데, 이어지는 4구 노드(r1b2)가 S 전위·P 후위로 되돌려 놓고 있었다. r1b2의 위치를 교대 뒤 상태(P 네트 앞, S 후위)로 바꾸고 대형 표기와 설명 줄(bh_rot)을 추가. 연속 노드 간 선수 이동을 전부 훑는 점검(2m 초과 이동 목록)으로 다른 되돌아감이 없음을 확인 — 나머지 큰 이동은 리프트 뒤 좌우 분리, 코너 이동, 교대 등 화살표·설명과 맞는 것뿐.
- 사용자 지적 반영(2026-09-23, "R, RP 나란히 있다가 R쪽 클리어니까 R이 받아야지?"): 리프트·클리어를 받는 쪽이 좌우(수비) 대형일 때 생성기가 항상 "후위"(둘의 깊이가 같으면 선수 순서상 두 번째)를 코너로 보내 반대쪽 선수가 가로질러 받고 있었다(서브 측 D 리프트→클리어, 리시브 측 4 리프트→상대 클리어). 수정: 받는 쪽은 목표에 가까운 선수(좌우면 그쪽, 앞뒤면 후위)가 뒤로 가서 치고 다른 한 명이 앞으로 나가 앞뒤 재구성(bb_pos·bh_rot). 점검 도구 `node scripts/check-moves.mjs [임계값] [topicId]`(연속 노드 간 임계값 초과 이동 목록).
- 예정: 비율 숫자 일괄 조정(사용자), 좌표 미세 조정, 4구 이후 확장.

### v0.8 (2026-09-23): 용어 정리 (남복 · 코스 선택)
- 사용자 지시 "전체 적용: 동성복식→남복": 종목 라벨(`SPORTS` level), 주제 제목 6개, 시작 화면·헤더(라벨에서 파생), 이 문서, 스크린샷 스크립트 7개(시작 화면 버튼 이름). 내부 id `level`은 화면에 안 보이므로 그대로.
- 사용자 지적 "갈래선택·코스선택·겨냥선택 다 같은 코스선택 아닌가": 초구 기술 버튼의 "→ 겨냥 선택"(드라이브, 서브 측·리시브 측 2곳)은 "→ 코스 선택"과 같은 단계(다음 화면에서 코스를 고름)라 "→ 코스 선택"으로 통일. "갈래 선택"은 다음 버튼 자리 안내(비활성)와 시작 화면 안내문에 쓰는 일반어로, 기술 선택 화면과 '누가 서브하나' 화면에도 뜨므로 코스 선택이라 쓰면 앱이 가르치는 '기술 → 코스' 구분과 어긋남 → 그대로 두고 사용자 판단 대기.
- 비율 숫자: 사용자 결정(2026-09-23) "현재 유지" → 위 예정 항목에서 뺌.
- 확인: `check-data.mjs` 문제 0(노드 378·노트 932·미검증 0), `scripts/shot-v08.mjs` 4장 page errors none — 시작 화면 남복 버튼, 헤더 "남복 · B조 ▾", 탭 6개 "남복 · …", 초구 갈래 3개 모두 "→ 코스 선택", 본문에 동성복식·겨냥 선택 0건, 버전 "초안 v0.8 · 용어 정리(남복 · 코스 선택)".
- v0.8.1(같은 날, 사용자 지시): 다음 버튼 자리 안내 "갈래 선택" → "선택"(시작 화면 안내문도 "선택 화면"). 시작 화면 "혼복" 버튼 비활성(`SPORTS[].disabled: true`, `.choice:disabled` 흐리게) — 사용자: 아직 미완성이라 나중에 활성화. 혼복 데이터·탭 코드는 그대로(플래그만 지우면 복귀). 확인: `scripts/shot-v081.mjs` 3장(초기·강제 클릭 후에도 혼복 미선택, 갈래 화면 하단 버튼 "선택", 설정 재오픈에서도 비활성).
- 사용자 제안(2026-09-23, 검토 중): 급수가 높을수록 네트샷(헤어핀) 코스 깊이·각도가 달라야 한다(A급은 더 타이트). 현재는 급수가 갈래 비율만 바꾸고 착지점·궤적은 고정(네트샷 착지 네트에서 0.8~0.9 m, 높이 low). 급수별 착지 깊이 보정 설계를 제안하고 사용자 승인·숫자 대기.
- v0.8.2(같은 날, 사용자 지시): 주제 이름 "플릭(롱) 서브" → "롱 서브"(서브 측·리시브 측 탭 2개, 스크립트 shot-flick.mjs 탭 선택 정규식 2곳). 샷 제목·노트의 "플릭"은 처음엔 두고 사용자에게 되물었으나, 에디터 지적(용어 변경 범위를 사용자가 좁히지 않았는데 내가 좁혀 재확인 요구)으로 v0.8.3에서 전부 통일.
- v0.8.3(같은 날): 화면에 보이는 "플릭"을 전부 "롱 서브"로 — 샷 제목("0 롱 서브", "상대 롱 서브", 혼복 "상대 롱 서브 → …"), 코트 위 샷 라벨("롱서브"), 갈래 버튼("롱 서브 → 여자/남자 본인이 뒤로"), 노트 문장(조사 은/이/을→는/가/를 교정), 출처 배지 "BF 롱 서브"(원문은 flick serve, 링크 그대로). 내부 id(flick-serve-side, bf_flick, rw_flickIn 등)는 그대로. 확인: `scripts/shot-v083.mjs`.
- 예정: 좌표 미세 조정, 4구 이후 확장.

### v0.9 (2026-09-23): 급수별 네트샷 깊이 (사용자 제안 승인 "진행해")
- 데이터: `NET_DEPTH = { D: 1.5 m·mid, C: 1.2·low, B: 0.9·low, A: 0.5·low }` + 급수별 설명 한 줄. 데이터의 `shot.to`는 B조 기준값이고 방향(부호)만 쓴다. 새 출처 태그 `"추"`(추정) 추가 — 급수별 숫자는 코칭 출처에 없어 추정한 값, 결과 문장("타이트=상대가 올림 / 느슨=공격권 넘김")만 bb_atk_angles·bh_place.
- 뷰어: `isNetShot`(type에 '네트샷') + `shotView(shot)`가 끝점 y를 `sign(y)·depth`로 바꿈. 적용 지점: 샷 재생(playShot), 갈래 미리보기(branchShots().map(shotView)), 설명 줄(renderPanel에 kind '급수' li, 배지 추정·BB 공격각·BH 코스), 급수만 바꿀 때 네트샷 화면이면 설명·경로 다시(applySettings). 시작 화면 안내문 "비율(%)과 네트샷의 깊이를 바꾼다". 받는 선수 위치는 데이터(B조 기준) 그대로 — 깊이 차이 0.4~0.6 m라 그대로 둠.
- 범위: 네트샷(초구 C/3 코스, 3구 네트샷·블록의 n_* 코스 중 type이 네트샷인 것, 4구, 혼복 네트샷) 전부. 블록·숏서브·리프트·스매시는 미적용(사용자가 네트샷을 보고 넓힐지 정함).
- 확인: check-data(NET_DEPTH 급수별 depth>0·height·text 검사 추가) 문제 0·노드 378; `scripts/shot-v09.mjs` 6장 page errors none — 코스 미리보기 끝점 B조 y=0.9 → D조 1.5, 스트레이트 네트샷 경로 끝점 D 1.5 / B 0.9 / A 0.5, 급수 설명 줄 각 급수 문장 확인, 3구 화면 미리보기의 네트샷 코스 끝점 −0.5(A조). 시트 2장 눈으로 확인.
- 실수 기록: 데이터 구간 주석에 "// 뷰어…"라고 써서 check-data의 구간 끝 표식과 충돌 → 노드 124로 잘못 집계. 주석을 "화면"으로 고침. 교훈: 데이터 구간 안 주석에 '// 뷰어' 금지.
- 다음(사용자 제안, 설계 승인 대기): 오른손 기준 포핸드/백핸드 판정 → 받는 면 × 급수에 따라 3구 기술 갈래 비율·코스 선택 보정. 별도 절에서 설계 후 진행.
- 예정: 좌표 미세 조정, 4구 이후 확장.

### v0.9.1 (2026-09-23): 네트샷 깊이 → 응수 보정 (리뷰어 지적 wgr_3f0dea25 반영)
- 문제: v0.9는 깊이를 샷 표시에만 적용해, A조 설명("상대는 올릴 수밖에 없다") 바로 다음 3구 선택 화면(C1s_3 등)에서 킬이 60%로 가장 높게 나왔다(w3·skill+1). D조도 설명("킬하기 쉽다")과 반대로 킬이 23%로 가장 낮았다. 설명과 응수 학습이 다른 내용을 가르침.
- 설계(상황 보정 1단계): `NET_DEPTH[급수].reply = { kill, net, lift }` 배수를 `w × SKILL_MULT` 위에 곱한다. **0 = 잠금**(그 급수에서 고를 수 없는 기술: 버튼 비활성 + '불가', 미리보기 화살표·% 생략). 값(추정, 태그 "추"): D 킬2.0/네트0.7/리프트0.6, C 1.2/1/0.8, B 0.5/1/1, A **0**/1/2. 결과(C1s_3, 킬/네트샷/리프트): D 48/23/29, C 42/30/28, B 33/37/30, A 불가/50/50. A조 설명은 "킬을 못 하고 되놓거나 올린다"로 고침(되놓기도 정석이라 "올릴 수밖에"보다 정확).
- '할 수 있는 선택지'와 '비율'을 구분(에디터 지적): 잠금(0)은 선택지 자체가 없어지는 것, 0<배수<1은 드문 선택. 같은 표 한 곳에서 둘 다 정한다. 포핸드/백핸드 보정(다음 단계)도 같은 형식으로 제안.
- 데이터 배관: 생성기가 3구 기술 선택 노드에 `incoming: <직전 노드 id>`(받아치는 셔틀), 각 기술 갈래에 `tech: <THIRD.techs 키>`를 찍는다. 뷰어 `incomingShot(node)` = node.shot 또는 incoming 노드의 shot; `branchMult(node, b)`가 배수, `isLocked`가 0 판정. 적용: branchProbs·renderBranches(disabled·불가)·drawBranchPreview(생략)·renderPanel(급수 줄을 받아치는 선택 화면에도)·applySettings(급수만 바꿔도 갱신).
- 버그 수정: `isNetShot`이 `/네트샷/`이라 공수 전환 t3kill의 type "킬/네트샷"도 네트샷으로 잡혀 킬 경로가 깊이 값으로 짧게 그려지고 급수 줄이 붙었음 → `/^네트샷/`.
- 적용 범위: 네트샷 뒤 3구 기술 선택 6곳(서브 측 C1s/C1/C1x, 리시브 측 r3s/r3/r3x — 후자는 상대의 응수). 나머지 네트샷 노드는 end라 갈래 없음. 손으로 쓴 갈래(초구·4구·랠리 주제)에는 tech 표식이 없어 보정 없음.
- 확인: check-data(reply 배수·incoming 참조 검사 추가, problems 선언 순서 버그 수정) 문제 0·노드 378; `scripts/shot-v091.mjs` — C1s_3 급수별 버튼·화살표 수·급수 줄, A조 킬 강제 클릭 무시, r3_3, t3kill 경로 끝점 -2.6. 시트 눈으로 확인.
- 예정: 좌표 미세 조정, 4구 이후 확장.

### v1.0 (2026-09-23): 포핸드/백핸드 상황 보정 (사용자 승인 "권장안으로 진행")
- 판정(`judgeSide`, 전원 오른손 가정): 받는 선수의 준비 위치 기준으로 셔틀 도착점이 바라보는 방향의 오른쪽 `SIDE_MARGIN`(0.5 m) 밖이면 포핸드, 그 안(몸 쪽)·왼쪽이면 백핸드. height "high"는 오버헤드: 왼쪽 0.5 m 밖 → 백핸드 오버헤드(라운드), 그 외는 돌아 들어가 포핸드. 우리(y>0)는 +x가 오른쪽, 상대(y<0)는 −x. 준비 위치 = 받는 셔틀이 날아오기 전 장면의 pos(3구는 코스 선택 화면, 4구는 생성기의 샷 전 위치 P[nearer]). 직전 노드(incoming)의 pos는 이미 셔틀 쪽으로 움직인 뒤(dx ≈ ±0.2)라 쓰지 않는다 — 덤프로 확인한 뒤 결정.
- 표(추정, 태그 "추"): `SIDE_MULT.low` 드라이브/푸시/블록/리프트 = D 0/0.7/1.2/1.6, C 0.6/0.9/1.1/1.3, B 0.9/1/1/1.1, A 1; `SIDE_MULT.high` 스매시/드롭/클리어 = D 0/0.8/1.4, C 0.4/1/1.2, B 0.8/1/1, A 1. 0 = 잠금(불가). 포핸드와 표에 없는 기술(킬·네트샷)은 1. 코스 가중 `BH_COURSE` = D 1/C 1.05/B 1.15/A 1.3 — 받는 선수가 백핸드가 되는 코스에 곱하고 라벨에 "(P 백핸드)"처럼 받는 선수 이름으로 표시. 스매시·킬 코스는 제외(bh_place가 몸·라켓 쪽 엉덩이를 겨냥하라고 하므로 백핸드 가중을 얹지 않음).
- 적용 지점: 3구 기술 선택 20곳(생성기가 `side` 스탬프 + kind "면" 설명 줄 + 프롬프트에 면), 4구 수비 갈래 2곳(스매시 센터·스트레이트 뒤 follow 갈래에 tech 키 + 받는 상대의 면), 코스 선택 76곳(생성 코스 선택 70 + 손으로 쓴 6곳에 `course: true` → `markCourses`가 갈래 138개에 백핸드 표시). 뷰어 `branchMult` = 네트샷 reply(v0.9.1) × SIDE_MULT × BH_COURSE; 0이면 잠금(v0.9.1 UI 그대로).
- 면 판정 결과(덤프 22곳, 전부 손으로 대조): 서브 측 A1 P백(왼쪽 2.0)·A1x P포·A1b S백(몸)·A1c P백·B1 S백(몸 0.4)·B1p P백(0.9)·C1s S백·C1 S백(몸)·C1x S포·D1 P백 오버헤드(2.2); 리시브 측은 좌우 대칭으로 같음(r1 P백 … r4 P백 오버헤드); 4구 D1 스매시 센터 → R백, r4 스매시 스트레이트 → R백(몸 0.5).
- 결과(사용자 예시 '초구 2-2 후위 쪽 드라이브 → P', 백핸드): D 드라이브 불가·푸시 26·블록 15·리프트 59, C 11/32/21/36, B 23/32/26/19, A 30/30/30/10. 'D 리프트 → P'(백핸드 오버헤드): D 스매시 불가·드롭 43·클리어 57, A 61/31/8. 푸시 코스 선택(B1p_3_push): D 66/17/17 → A 44/42/14(백핸드 코스 가중). 4구 스매시 센터 뒤 상대 수비(R 백핸드): D 블록 11/리프트 45/짧은 리프트 44, A 43/43/14.
- 확인: check-data(SIDE_MULT·BH_COURSE·side·course·oppSide 검사 추가) 문제 0·노드 378·노트 954(면 설명 22줄 추가)·면 판정 노드 22·백핸드 코스 갈래 138; `scripts/shot-v10.mjs` page errors none; 시트 눈으로 확인.
- 실수 기록: 생성기 안에 이미 `side`(us/them) 변수가 있어 같은 이름으로 선언해 SyntaxError → `hitSide`로. 4구 follow 갈래 문구가 옛 D2 노드와 같아 한 줄 앵커가 2건 매치 → follow 블록 전체를 앵커로.
- v1.0에서 미적용이었던 곳(v1.1에서 적용): 초구(서브 리턴) 기술 선택 2곳과 랠리·롱 서브·4구의 손으로 쓴 기술 갈래 10곳. 처음에 '범위 밖(다음 결정)'으로 적었으나 사용자 요청은 '전체적으로'였고 제외 합의가 없었으므로(에디터 지적) 같은 요청의 남은 부분으로 v1.1에서 적용. 코트 위 선수 아이콘에는 면 표시를 넣지 않음(프롬프트·설명 줄로 표시).
- 예정: 좌표 미세 조정, 4구 이후 확장.

### v1.1 (2026-09-23): 포핸드/백핸드 전 구간 적용 (에디터 지적 반영 — '전체적으로' 요청의 남은 부분)
- 대상: 손으로 쓴 기술 선택 12곳 — 서브 측 초구 응수(reply, 리시버 R), 4구 D3b·D3c(P), 리시브 측 초구 선택(choice, R), 롱 서브 fserve(R)·fA1(P)·rflick(R), 드라이브 랠리 dr2f·dr2low(F), 공수 전환 t1(O1)·t2drive(B)·t4smash(B). 갈래 34개에 `tech` 표식(리시브 미스 갈래 제외), 샷 없는 선택 노드(reply/choice/dr2f/dr2low/t1)에 `incoming`(오는 샷 노드).
- 판정 함수 `markSides(topic)`(데이터 구간, buildThird 뒤·markCourses 앞에 실행): 갈래에 tech가 있고 side가 없는 노드마다 받는 선수 = 갈래 샷의 타자(전부 같아야 하며 다르면 throw), 오는 샷 = 자기 shot 또는 incoming의 shot, 준비 위치 = 오는 샷 노드의 앞 노드 pos(3구 생성기와 같은 규칙; 앞 노드가 없으면 자기 샷이면 topic.initial, 아니면 오는 샷 노드의 pos) → `side` + kind "면" 설명 줄 + 프롬프트 꼬리 "(R 백핸드)".
- 코스 면: 갈래가 전부 샷으로 이어지는 손으로 쓴 노드 10곳과 생성기의 4구 follow 잎 2곳에 `course: true` → markCourses가 받는 상대의 백핸드 코스에 BH_COURSE와 "(O1 백핸드)" 표시. markCourses의 받는 선수는 다음 노드가 side.player를 정해 뒀으면 그 선수, 아니면 도착점에 가장 가까운 상대(v1.0 규칙).
- 판정 결과(덤프 12곳, 손으로 대조 일치): reply R백(몸 −0.1), choice R백(몸 −0.2), D3b P포(오버헤드 4.3), D3c P포(3.1), fserve R포(0.4), fA1 P백(몸 0), rflick R포(0.4), dr2f·dr2low F백(몸 0.2), t1 O1백(왼쪽 −1.5), t2drive B포(2.9), t4smash B백(몸 −0.1). 새 백핸드 코스 라벨 14개(예: fA1 깊은 리프트 → R 백핸드 코너, dr2f 푸시 틈 → OF 백핸드, t4smash 블록·푸시 → O1 백핸드).
- 화면 결과(추정 비율): 서브 측 초구 응수 D조 푸시 10·드라이브 불가·네트샷 29·리프트 46·미스 15(A조는 38/25/25/8/4로 v0.6과 같음); 리시브 측 초구 D 12/불가/34/54, A 39/26/26/9; 드라이브 랠리 F 선택 D 불가/78/22, A 42/42/16; 공수 전환 상대 수비 D 25/불가/75, A 31/46/23; 스매시 수비(B 백핸드) D 24/불가/4/72, A 29/33/21/17; 롱 서브 스매시 수비(P 백핸드) D 33/67, A 61/39(리프트 코스가 R 백핸드라 가중).
- 문구: 면 설명 줄의 높은 공 포핸드 문장(v1.1에서 처음 화면에 나옴)을 "오른쪽 뒤로 높게 온다" / "머리 위로 높게 온다 — 돌아 들어가 포핸드로 칠 수 있다"로 다듬고, 조사 은/는을 선수 기호 끝소리에 맞춤(R은·O1은).
- 확인: check-data(incoming은 next 또는 branches로 이어지면 통과, tech 갈래에 side 필수, side.player = 갈래 샷 타자 검사 추가) 문제 0·노드 378·노트 966·면 판정 노드 34·백핸드 코스 갈래 152; `scripts/shot-v11.mjs` 10장 page errors none(D조 잠금 갈래 강제 클릭 무시 확인); 시트 2장 눈으로 확인; 터널 v1.1. 백업 /tmp/index.v1.0.bak.html.
- 데이터에서 드러난 것(사용자 판단): 공수 전환 첫 스매시가 상대 이음새([0.1,-3.9])로 가는데 데이터는 O1이 받게 되어 있어 O1 백핸드(왼쪽 1.5 m)로 판정됨. 거리로는 O2 포핸드(1.3 m)가 더 가까움. 누가 받게 할지는 시나리오 내용이라 사용자 결정(O2로 바꾸면 이어지는 상대 응수 3갈래의 O1/O2 역할도 바뀜). → 사용자 확인 "포핸드가 받는게 맞음": v1.1.1에서 O2로 변경.
- 초구 리턴 잠금에 대한 유보: 숏서브 리턴은 네트 앞에서 백핸드 그립으로 처리하는 일이 많아 D조 '드라이브 불가'가 실제보다 강할 수 있음(v1.0에 적었던 우려). 표는 승인된 대로 두었고, 숫자는 사용자가 클럽 경험으로 조정.
- 실수 기록: v1.0 보고에서 초구·랠리를 '승인 범위 밖'으로 적고 다시 물음 — 사용자의 '전체' 요청을 내 제안 범위로 좁힌 것. 이후로는 요청 범위가 기준이고 제안이 좁았으면 나머지를 이어서 한다.

### v1.1.1 (2026-09-23): 공수 전환 — 이음새 스매시는 포핸드 쪽 수비수가 받음 (사용자 확인 "포핸드가 받는게 맞음. 숫자는 권장안으로 진행")
- 바뀐 것: 공수 전환 첫 스매시(B → [0.1,-3.9])를 받는 수비수를 O1(백핸드)에서 O2(포핸드)로. 상대 응수 3갈래(블록 t2block·드라이브 카운터 t2drive·리프트 t2lift)의 타자 O1→O2, 타점 [-0.2,-3.8]→[0.2,-3.8], 위치는 O1 제자리([-1.4,-4])·O2 이음새([0.2,-3.6]/[0.2,-3.8]). 이어지는 t3kill(O2 [1.0,-3.4])·t3flat(O2 [0.9,-3.9])은 O2가 이음새에서 돌아오는 위치로, t3ourlift·t4smash 이후는 그대로(우리 리프트가 O2 쪽 코너라 O2가 후위로 가서 스매시). t2drive 코스 노트를 "크로스 수비수(O2)는 스트레이트로, 스트레이트 수비수는 크로스로"(bb_def_angles 같은 원문 범위, 방향만 O2 기준)로. t1에 사용자 확인 노트(태그 "확"): "두 수비 사이(이음새)로 온 셔틀은 포핸드 쪽 수비수(O2)가 받는다(오른손 기준). O1에게는 같은 공이 백핸드 쪽."
- 판정: t1 side = O2 포핸드(dx 1.3, 준비 위치 t0.pos.O2 [1.4,-4]) → 기술 제한 없음. 상대 수비 비율(추정) D조 블록 25/드라이브 불가/리프트 75 → 27/13/60(드라이브 카운터 잠금 해제), A조 31/46/23 그대로. B의 드라이브 맞받기 화면 D 25/75, 4구 수비 화면 24/불가/4/72 그대로.
- 숫자: "숫자는 권장안으로 진행" → SIDE_MARGIN 0.5·SIDE_MULT·BH_COURSE·NET_DEPTH 변경 없음(v1.0 권장안 그대로, 추정 태그 유지). 초구 리턴 D조 드라이브 잠금도 그대로.
- 확인: check-data 문제 0·노드 378·노트 967(확 태그 1줄 추가)·면 판정 노드 34; check-moves(2 m 초과)에서 새로 생긴 이동은 t2drive→t3ourlift O2 2.5 m(이음새에서 뒤 코너로, 높은 리프트 동안) 1건 — 나머지는 이전과 같음; `scripts/shot-v111.mjs` 6장 page errors none(t1 D 27/13/60·A 31/46/23, 프롬프트 '(O2 포핸드)', 확인 배지 노트, t2drive 25/75, t4smash 24/불가/4/72, 버전 v1.1.1); 시트 2장 눈으로 확인; 터널 v1.1.1 응답. 백업 /tmp/index.v1.1.bak.html.

### v1.2 (2026-09-23): 선수 이름 한글 (사용자 "S,P,R,RP가 잘 와닿지 않아. 한글로 변경? 추천해줘" → 추천안 → "그대로 적용")
- 이름표(승인): 서브 4주제 S=서버, P=서버 파트너, R=리시버, RP=리시버 파트너. 드라이브 랠리 F=나, B=파트너, OF=상대 전위, OB=상대 후위. 공수 전환 B=나(선택하는 사람), F=파트너, O1=상대1, O2=상대2. 혼복 2주제(잠금)는 이름표 없음 → id 그대로.
- 왜 이렇게: 서버·리시버·파트너는 서브 때 정해진 역할이라 그 뒤 앞뒤·좌우가 바뀌어도 이름이 맞는다. 랠리 주제는 두 선수 다 역할 이름이 없고, 전위/후위를 이름으로 쓰면 공수 전환의 역할 교대 장면(블록 뒤 전진 t5block, 재공격 t6reattack)에서 '후위'가 앞에 서는 그림이 된다 → 선택하는 사람을 '나'로(드라이브 랠리는 F, 공수 전환은 B). 상대는 드라이브 랠리에서 앞뒤가 안 바뀌므로(14장면 전부) 전위/후위, 공수 전환은 좌우↔앞뒤가 바뀌므로(t5drive 등) 번호.
- 구현: 주제마다 `names` 필드(표시 이름). 내부 id(S·P·R·RP·F·B·O1…)는 데이터 구조·검사·노드 이름에 그대로 남는다. `nameOf(topic,id)`(글), `labelOf(topic,id)`(코트 원 = 이름의 마지막 낱말: 파트너/전위/후위; 팀은 원 색으로 구분), `josa(name, kind)`(받침에 맞춘 이/가·은/는·을/를·과/와·으로/로·의, "나"는 내가·내). 코트 원은 3글자 이상이면 글꼴 0.34→0.24. 생성 문장(면 판정 줄 sideNote, 코스 라벨 markCourses, 질문 꼬리 markSides, 3구 생성기 노트·질문·제목·화살표, 4구 follow 질문)은 전부 이름표+조사 함수를 거친다.
- 손으로 쓴 글: 서브 4주제·드라이브·공수 전환·3구 spec의 설명·제목·갈래·질문·화살표·대형·끝 문구 229줄을 스크립트(/tmp/patch/apply_v12.py)로 치환. 규칙: 역할 낱말 뒤 괄호 id("서버(S)"→"서버", "파트너(RP)"→"리시버 파트너", "후위(P)"→"후위(서버 파트너)"), 겹침 제거("상대 전위 OF"→"상대 전위", "OF 전위"→"상대 전위"), 조사 교정(R이→리시버가, R은→리시버는, F가→내가, F의→내), '나' 소유("F 몫"→"내 몫", "후위 B 몫"→"후위인 내 몫", "B(후위)가"→"후위인 내가", "F 머리 위"→"내 머리 위"). 급수 "B조"·갈래 이름 "F-A/B-1"·초구 갈래 "B 드라이브"는 선수 id가 아니라 그대로. 문장 하나는 관점을 고침: 공수 전환 t2block "블록은 상대 전위(F)에 대한…"(상대 관점) → "블록은 우리 전위(파트너)에 대한…".
- 검사: check-data에 이름표 검사 추가 — 이름 있는 주제는 선수 전원 이름이 있어야 하고, 생성 후 화면 글(제목·질문·끝 문구·대형·화살표·노트·갈래 라벨)에 그 주제 선수의 약자가 남으면 문제. 결과 주제 8(이름표 6)·노드 378·노트 967·문제 0. 조사 오류 grep(서버이/파트너은/상대1가 등) 0.
- 화면: `scripts/shot-v12.mjs` 16장 — 코트 원 라벨(서버/파트너/리시버/파트너, 나/파트너/전위/후위, 상대1/상대2), 질문 꼬리 "(리시버 백핸드)"·"(나 포핸드)", 면 설명 줄 "리시버는 백핸드로 받는다 … 리시버의 몸 쪽으로", 코스 라벨 "(서버 파트너 백핸드)", 3구 제목 "3구: 서버 파트너 푸시 → …", 대형 "톱앤백(역할 교대: 나 전위·파트너 후위)". 스크립트가 화면 글 전체에서 약자 잔존을 검사한다.
- 한계: 버튼 라벨이 길어짐("스트레이트 트램라인 (기본) (리시버 파트너 백핸드)") — 두 줄로 접히지만 읽힘. 혼복 주제는 잠금이라 S/SP/R/RP/W/M 그대로(활성화할 때 이름표 추가).

### v1.2.1 (2026-09-23): 종목 라벨 '남복/여복' + 탭 이름에서 종목 접두 제거 (사용자 "첫 화면 남복에서 남복/여복으로 바꿔. 게임화면에서 탭에 '남복'으로 시작되는데 삭제해. 어차피 최상단에 표시되어 있잖아. 중복이네.")
- 종목 라벨: `SPORTS[0].label` "남복" → "남복/여복". 이 한 곳이 시작 화면 종목 버튼과 헤더 버튼을 같이 먹이므로 헤더도 "남복/여복 · D조(초급) ▾"가 된다(단일 출처 유지). 데이터·비율·이름표는 그대로 — 남복 시나리오를 여복에도 같은 내용으로 쓴다는 뜻이고 여복 전용 내용은 없다.
- 탭 이름: 주제 `title`에서 종목 접두를 뗌 → "숏서브 · 서브 측", "숏서브 · 리시브 측", "롱 서브 · 서브 측", "롱 서브 · 리시브 측", "드라이브 랠리", "공수 전환 랠리". 잠금 상태인 혼복 2주제도 같은 이유(종목은 헤더 버튼에 이미 보임)로 "숏서브 · 서브 측", "숏/롱 서브 · 리시브 측"으로. 종목 구분은 `sport` 필드가 맡는다(탭은 종목별 필터라 같은 제목이 한 화면에 겹치지 않음).
- 스크립트: 옛 확인 스크립트의 `getByRole("button", { name: "남복" })`은 부분 일치라 "남복/여복"에도 맞아 그대로. `shot-mixed.mjs` 탭 정규식 2곳에서 "혼복 · " 제거. 새 확인 스크립트 `scripts/shot-v121.mjs`(시작 화면 버튼·헤더 버튼 폭·탭 목록·혼복 title).
- 확인: check-data 문제 0(주제 8·노드 378·노트 967); shot-v121.mjs 5장 page errors none — 시작 화면 버튼 ["남복/여복","혼복[disabled]"], 헤더 "남복/여복 · D조(초급) ▾"(버튼 오른쪽 끝 378/390px, 높이 30px, 넘침 없음; A조도 같음), 탭 6개 제목 그대로, 혼복 title 2개, 종목 접두 남은 탭 0. 시트 눈으로 확인(시작 화면 두 버튼, 헤더 한 줄, 탭 줄).

### v1.3 (2026-09-24): 설명 줄 간결화 — 기본·면·조건·상황 (사용자 "기본, 면, 조건, 상황에 대한 문장이 너무 길어서 간결하게 핵심만 해줘.")
- 대상: 노트 종류 기본(74줄)·조건(54줄)·상황(54줄)의 손으로 쓴 문장 158줄 + 생성기 안 기본 문장 2곳 + 면 문장 형식(`sideNote`). 다른 종류(코스·대형·로테이션·위험·목적·규칙)는 지시에 없어 그대로 — 같은 방식으로 줄일 수 있다.
- 규칙: 글만 줄이고 뜻·출처 키·종류는 그대로. 조건·예외를 나타내는 말("늦으면", "네트 아래면", "가능하면")은 남기고 근거 설명·반복·괄호 부연을 뺐다. 원문보다 강한 단정이나 새 주장은 넣지 않음(바뀐 표현은 "올릴 수밖에 없다"→"올린다"처럼 약해지는 쪽만). 리뷰어 정정 수치 60~90cm는 유지하고 "(2~3피트)" 병기만 삭제. 사용자 확인 노트(공수 전환 t1, 태그 "확")도 뜻 그대로 줄임.
- 면 문장: "리시버는 백핸드로 받는다(오른손 기준, 추정) — 셔틀이 리시버의 몸 쪽으로 온다. 급수가 낮을수록 백핸드 드라이브·푸시가 제한되고(D조 드라이브 불가) 블록·리프트가 는다." → "리시버는 백핸드(몸 쪽, 오른손 기준). D조 드라이브 불가, 낮은 급수는 블록·리프트가 는다." 추정 표시는 배지('추')가 맡는다. 포핸드: "리시버는 포핸드(오른쪽, 오른손 기준). 기술 제한 없음." 머리 위 포핸드: "(머리 위, 돌아 들어감, 오른손 기준)". 백핸드 오버헤드: "D조 스매시 불가, 낮은 급수는 클리어가 는다."
- 결과(고유 문장 평균/최대 글자): 기본 60/104 → 45/58, 면 90/116 → 45/58, 조건 65/123 → 44/58, 상황 60/106 → 44/57. 60자 초과 0. 새 통계 도구 `scripts/note-stats.mjs`(종류별 길이 + 괄호 짝·두 칸 띄어쓰기 검사).
- 확인: check-data 문제 0(주제 8·노드 378·노트 967·미검증 0 — 노트 수·출처 키 그대로); `scripts/shot-v13.mjs` 9장 page errors none(화면의 네 종류 노트 전부 60자 이하), 시트 2장 눈으로 확인(서브 0·초구 응수·3구·3구 잎·리시브 초구·롱 서브·드라이브 선택·공수 전환 t1·t4smash). 패치 /tmp/patch/apply_v13.py(줄 번호 + 원문 일치 assert, 새 문장이 더 길면 실패), 백업 /tmp/index.v1.2.1.bak.html.

### v1.3.1 (2026-09-24): 리뷰어 지적 반영 — 간결화하며 빠진 조건·예외 복원 (wgr_3c21e9505b5b8ba489f90a1f)
- 지적: 리시브 측 준비 노드의 조건 줄에서 롱 서브 대응 "균형 잃으면 클리어"가 사라져 "일찍 읽으면 공격, 늦으면 드롭"만 남음 — 부연이 아니라 상황별 기술 선택을 바꾸는 핵심 예외. v1.3 완료 근거("조건·예외 유지")와 실제가 어긋남.
- 조치: 그 줄을 복원하고, 같은 유형을 158줄 전체에서 다시 훑어 37곳 복원 — 균형 잃으면(리시버 초구·혼복 리시브 포함 3곳), 일찍 잡았고(뒤 코너 조건 2곳), 네트 위·여유 있으면/타이트(높이 규칙 7곳), 타이트한 서브에 블록/네트샷(2곳), 붙일 수 있으면 스핀 네트샷 선택지, 리프트의 대가(상대가 계속 스매시)·종종(2곳), 좋은 스매시가 있으면 스매시 우선, 라켓 쪽 엉덩이, 친 쪽으로, 타이트 네트샷(4곳), 리프트 → 즉시 좌우, (앞뒤 유지)(2곳), 넓게 온 서브는 어렵다, 여자는 종종 앞에, 덮치지 못할 만큼, 앞으로 준비, 숏서비스라인 조금 넘게(2곳), 서버 옆을 지나면, 거의 언제나, 리시버 옆을 지나면. 면 문장에 "드라이브·푸시 ↓"(낮은 급수에서 푸시도 준다는 내용) 복원: "리시버는 백핸드(몸 쪽, 오른손 기준). D조 드라이브 불가, 급수 낮을수록 드라이브·푸시 ↓ 블록·리프트 ↑." / 오버헤드 "D조 스매시 불가, 급수 낮을수록 스매시 ↓ 클리어 ↑."
- 감사 방법: 전 파일(v1.2.1 백업)과 지금 파일의 같은 순서 노트끼리 조건 표현(…면/때/수록/만/뿐/지만/거나/않)과 완화어(보통·종종·가끔·거의·쯤 등)를 비교해 사라진 것을 목록으로 뽑고 한 줄씩 판단(표현만 바뀐 것은 통과, 뜻이 빠진 것은 복원).
- 결과(고유 문장 평균/최대 글자, 60자 초과): 기본 47/66(3), 면 50/65(6), 조건 46/63(3), 상황 45/65(2). v1.3보다 조금 길어졌지만 v1.2.1(평균 60~90)의 절반대. check-data 문제 0.
- 교훈: 줄일 때 조건·예외·선택지는 '부연'이 아니다. 문장 안의 열거(A/B/C)와 조건절 수를 전후로 세어 같은지 확인한 뒤 완료라고 적는다.

### v1.4 (2026-09-24): 설명 줄 출처 배지·범례 제거, 접기 제목 '출처'
- 사용자 지시: "'출처 목록'을 '출처'로 문구 변경. 출처 목록에 이미 출처가 있으므로 목적 조건 등 내용에 출처 제거" → 이어서 "범례 문단('출처 배지를 누르면 원문…좌표·설명 전부 초안.') 삭제, 불필요" → "출처에 '(무엇을 확인했나)' 삭제".
- 화면: 설명 줄(기본·조건·상황·목적·면·급수 등 모든 kind) 끝에 붙던 출처 배지(브리프/확인/추정/링크 배지)를 뺐다. 범례 문단 삭제. 접기 제목 "출처 목록 (무엇을 확인했나)" → "출처". 출처는 화면 아래 접기 하나에서만 본다(항목 25, 링크 21 — 그대로).
- 코드: 노트 렌더 2곳(일반 노트, 네트샷 뒤 '급수' 줄)에서 srcBadges 호출 제거 → 쓰는 곳이 없어진 srcBadges 함수와 .src CSS 5줄, .legend CSS 삭제. 데이터의 notes[].src 키와 SOURCES 표는 그대로(check-data의 미검증 집계, 출처 접기, 이 문서의 추적용).
- 검증: check-data 문제 0(주제 8, 노드 378, 노트 967, 미검증 0). scripts/shot-v14.mjs — 13화면에서 설명 줄 안 배지/링크 0, 범례 0, 접기 제목 '출처', 출처 접기 펼침 항목 25·링크 21, page errors 없음. 시트 2장 눈으로 확인.

### v1.5 (2026-09-24): 코트 라인 표시 — 단식/복식 인·아웃 띠 (사용자 "단식과 복식은 코트 라인이 다르지? 표현해줘 초보 사용자는 잘 모를테니")
- 화면: 코트 위에 지금 장면에서 아웃인 띠를 어둡게 칠하고(fill-opacity .42), 코트 바로 아래 한 줄(court-note)로 설명.
  - 복식 서브 장면(shot.type에 '서브'): 리시브 쪽 뒤 띠(롱서비스라인~끝선, 0.76 m)만 칠함 — "복식 서브: 칠한 뒤 띠는 아웃(끝선 앞 선까지)." 우리가 서브하면 위쪽, 상대가 서브하면 아래쪽.
  - 복식 랠리: 띠 없음 — "복식: 바깥 옆선 안, 끝선까지 인(뒤 띠는 서브 때만 아웃)."
  - 단식(sport "singles"): 양쪽 옆줄(단식 옆선~복식 옆선, 0.46 m)을 항상 칠함 — 서브 "단식 서브: 칠한 옆줄은 아웃, 뒤는 끝선까지 인." / 랠리 "단식: 안쪽 옆선 안이 인, 칠한 옆줄은 아웃."
- 근거: BWF 규칙 1.1 코트 도면(단식 옆선 안쪽, 복식 바깥쪽), 9.1.7(서브는 리시버의 서비스 코트 안에 착지 — 단식은 끝선까지, 복식은 롱서비스라인까지). 출처 키 bwf_laws.
- 코드: gZones 그룹(코트 선 위, 화살표·선수 아래), drawZones(node)를 render 첫머리에서 호출. 데이터 변경 없음.
- 검증: check-data 문제 0. scripts/shot-v15.mjs — 복식 서브 측 0(띠 1, 위), 서브 측 1(띠 0), 리시브 측 0(띠 0)·1(띠 1, 아래), 롱 서브 0(띠 1); 단식은 아직 데이터가 없어 /tmp/preview(스텁 주제 1개 + SPORTS에 단식)로 확인 — 시작 화면 버튼 '남복/여복 · 단식 · 혼복(잠금)', 헤더 '단식 · D조(초급) ▾', 서브·랠리 화면 띠 2(양 옆줄). page errors 없음. 스텁은 배포 파일에 넣지 않음.

### 단식 추가 (2026-09-24 사용자 승인 "승인할게") — 계획
- 라벨 '단식'(남녀 구분 없음 — 내 제안, 출처에는 남녀 차이 언급 있음: 로우 서브는 고급 남자 단식에서 흔함(BB), 하이 서브 리턴 드롭 비율 남 56%·여 47%(BI)). 버튼 순서 남복/여복 · 단식 · 혼복. 첫 주제 하이 서브·서브 측.
- 순서: (1) 주제 1 트리를 글로 → 사용자 검토(docs/singles-topic1-tree.md) → (2) 데이터 + SPORTS '단식' + 화면 → (3) 주제 2~4(숏서브·리시브 측, 클리어·드롭 랠리, 스매시 수비→전환) 하나씩.
- 데이터 모델 추가 예정(2단계): 갈래 minTier(급수 미만 잠금 '불가'), 설명 종류 '이동'(베이스 복귀). 복식·혼복 데이터와 비율 표는 건드리지 않음.
- 출처(본문 확인 2026-09-24): BB 단식 서브·베이스·코너·중앙·클리어·드롭·네트샷·리프트·스매시·스매시 수비, BI 단식 리시브 6종·단식 수비, BH 리시브 3구 플랜, TeachPE 단식. 급수별 숫자는 출처에 없어 '추정'.

### v1.6 (2026-09-24): 단식 첫 주제 「하이 서브 · 서브 측」 — 급수 없음 (사용자 "단식은 급수 없이 진행. 구현해")
- 종목: SPORTS에 { id: "singles", label: "단식", noTier: true } (남복/여복 · 단식 · 혼복(잠금)). 단식은 급수 개념이 없다: 시작 화면에서 단식을 고르면 급수 블록(#tierBlock)이 숨고 바로 시작, 헤더 '단식 ▾', 비율은 갈래 비중 w만 정규화(급수 배수·잠금·네트샷 깊이·'급수' 설명 줄 없음). 판정 코드는 noTier() 한 곳으로 분기.
- 화면: 선수 2명 '나'·'상대'(내부 id ME/OP), 대형 줄 라벨 '나:/상대:', 코트 아래 라벨 '나'. 설명 종류 '이동'(베이스 복귀) 추가.
- 데이터(buildSinglesHighServe, 장면 96 = 선택 10·코스 21·마지막 55): 0 하이 서브(중앙 깊게) → 1 상대 리턴 종류(클리어 4·빠른 드롭 4·스매시 2·슬라이스 1) → 코스(9) → 2 상대 샷 장면 → 3 내 3구 기술 → 코스 → 마지막 장면(내 샷 + 결과 한 줄). 상대 리턴 코스와 내 3구 표는 docs/singles-topic1-tree.md(승인본) 그대로, 단 급수 잠금 3개(슬라이스 B조 이상·백핸드 오버헤드 스매시·백핸드 드라이브)는 급수가 없어져 빼고 비중만 둠(스매시는 백핸드 뒤 2 · 포핸드 뒤 3).
- 좌표 규칙: 내 포핸드 = 오른쪽(x>0), 상대는 마주 보므로 포핸드 = 우리 기준 왼쪽. 내 샷의 스트레이트/크로스는 타점 x 부호 기준으로 계산(dir "st"/"cr") — 포핸드 뒤에서는 스트레이트가 곧 상대 백핸드 뒤라 클리어 코스 표를 따로 둠(clearFH). 마지막 장면의 내 복귀: 네트샷 뒤 앞(3.2), 클리어·리프트·드롭 뒤 뒤(4.3)·친 쪽 0.4 치우침, 스매시·드라이브·푸시 뒤 절반만(BB 베이스).
- 출처 14개 추가(bb_serve_s, bb_base, bb_corners, bb_middle, bb_clears, bb_drops, bb_nets, bb_lifts, bb_smash_s, bb_smashdef, bi_ros_s, bi_def_s, bh_ros_s, tp_sgl — 각각 인용 문장 + 요약). 규칙 줄은 bwf_laws.
- 검증: check-data 문제 0(주제 9, 노드 474, 노트 1150). 단식 노트 60자 초과 0, 착지·위치 단식 선 안, 스트레이트/크로스 방향 검사 불일치 0, check-moves 3.5 m 초과 0(단식은 한 명이 코트를 다 덮어 2~3 m 이동이 정상). scripts/shot-v16.mjs 15화면: 급수 힌트·불가·급수 줄 없음, 대형 라벨 '나:', 코트 설명 '단식…', 복식 회귀(급수 블록·헤더·힌트에 D조) 통과.
- 남은 것: 앱 제목('복식 상황 학습')은 사용자 결정 대기. 주제 2~4는 3단계.

### v1.6.1 (2026-09-24): 리뷰어 지적 반영 — 상대의 빠른 드롭 뒤 복귀 방향 (wgr_023a820134084feeff0c46ce)
- 지적: 주제 1의 d_bh·d_fh 장면에서 상대가 빠른 드롭 뒤 '베이스를 앞으로 치우쳐 복귀'한다고 적고 배치했는데, 인용한 BB 베이스 원문은 클리어·리프트·(빠른) 드롭 뒤 '뒤로' 치우치라고 함. 내 드롭 뒤 복귀는 뒤로 되어 있어 같은 전술을 선수에 따라 반대로 가르침.
- 조치: 상대 복귀 위치 [-0.2, -3.2] → [-0.2, -4.4], 이동 문구 "상대는 빠른 드롭 뒤에도 베이스를 뒤로 치우쳐 복귀 — 리프트 대비.", 대형 줄 '베이스(뒤 치우침)'. 슬라이스 장면도 같은 원리(추정 태그)로 뒤. 복귀 규칙을 SGL 공통 도우미(recover·moveNote·formUs)로 모아 두 선수·두 주제가 같은 문구를 쓰게 함. 검사: '드롭 뒤 … 앞으로' 문구 0.
- 교훈: 출처를 인용한 문장은 인용 원문과 방향(앞/뒤·직선/크로스)까지 대조한다. 같은 전술을 나와 상대에게 다르게 적으면 오류.

### v1.7 (2026-09-24): 단식 주제 2 「숏서브 · 리시브 측」 (승인 계획 3단계, 에디터 지적으로 되묻지 않고 진행)
- 흐름: 0 준비(서비스라인 약 1.3 m 뒤, 라켓 앞) → 1 상대 서브 코스(T 4 · 몸 3 · 와이드 1 · 플릭 1) → 1 서브 장면 → 2 내 리턴(리프트 4 → 코스 3 / 네트샷 3 → 타이트·깊게 / 속임 백핸드 네트 크로스 1 / 푸시 1) → 2구 장면 → 3 상대 3구 예상(리프트 뒤: 클리어 3·드롭 3·스매시 2, 평평한 리프트 뒤: 가로채기 스매시 3·클리어 2·드롭 1, 타이트 네트 뒤: 되놓기 3·짧은 리프트 2·깊은 리프트 2·킬 1, 깊은 네트 뒤: 리프트 3·푸시 2·네트샷 2) → 3구 장면 + 내 4구 지침(끝). 플릭 서브는 뒤 코너(포핸드 뒤) → 클리어(스트레이트 = 상대 백핸드)·빠른 드롭·스매시(균형 잡혔을 때). 속임 네트·푸시는 2구에서 끝.
- 장면 108(선택 20·코스 8·마지막 60). 출처: BI 단식 리시브 6종, BH 리시브 3구 플랜, BB 단식 서브·네트샷·리프트·클리어·드롭·코너·중앙·베이스, 규칙 bwf_laws.
- 검증: check-data 문제 0(주제 10, 노드 582, 노트 1314). 단식 두 주제 노트 60자 초과 0, 선 밖 0, 스트레이트/크로스 방향 불일치 0, check-moves 3.5 m 초과 0. scripts/shot-v17.mjs 14화면(주제 1 정정 화면 포함) 급수/잠금 없음, page errors 없음.

### v1.8 (2026-09-24): 단식 주제 3 「클리어·드롭 랠리」·주제 4 「스매시 수비 → 전환」 — 승인 계획의 마지막 두 주제
- 주제 3(장면 75): 0 랠리 중 상대 클리어(내 백핸드 뒤) → 1 내 선택(클리어 스트레이트 3 · 크로스 클리어 2 · 빠른 드롭 스트레이트 3 · 크로스 1 · 공격 클리어 1) → 1구 장면 → 2 상대 응수 예상(내 샷별: 클리어 되받기·드롭·스매시 / 얕은 클리어·드롭·직선 스매시 / 네트샷·리프트·크로스 리프트 / 급한 클리어·가로채기 스매시) → 2구 장면 → 3 내 다음 샷(응수별: 반대 코너·같은 코너 반복·드롭 / 스매시·드롭 / 타이트 네트·리프트 코너·낮게 / 블록·높은 리프트 / 되놓기·리프트 / 스매시·드롭·클리어) → 마지막 장면(흔들기 판정). 근거: BB 무브먼트 프레셔(출처 bb_mp 추가)·코너·클리어·드롭·네트샷·리프트·스매시·베이스.
- 주제 4(장면 90): 0 내가 상대 백핸드 뒤로 리프트(수비 베이스를 리프트한 쪽으로, 라켓 무릎 높이, 스플릿 스텝 — BI 단식 수비) → 1 상대 스매시 코스(직선 3 · 몸 2 · 크로스 2) → 스매시 장면 → 2 내 수비(블록 → 코스 / 리프트 높게·낮게 / 드라이브 빈 쪽 / 롱 블록 크로스(직선 스매시 뒤만)) → 수비 장면 → 3 상대 응수 예상(블록 뒤: 네트샷·리프트·킬, 높은 리프트 뒤: 스매시·드롭·클리어, 낮은 리프트 뒤: 가로채기 스매시·급한 클리어, 드라이브 뒤: 블록·드라이브·스매시, 롱 블록 뒤: 푸시·리프트) → 응수 장면 + 전환 지침(끝: '전환 성공/중립/다시 수비').
- 공통 도우미 SGL.oppFollow(내 샷 장면 뒤 상대 응수 선택 → 응수 장면; opts[].follow로 한 단계 더 이어 붙임). 코스 좌표는 내 위치 기준 dir(st/cr)로 계산.
- 검증: check-data 문제 0(주제 12, 노드 747, 노트 1546, 미검증 0). 단식 4주제 노트 60자 초과 0, 선 밖 0, 스트레이트/크로스 방향 불일치 0(주제 3에서 1건 발견 → dir 지정으로 수정), 막힌 장면 0. check-moves 3.5 m 초과: 주제 3에서 5건·주제 4에서 3건 — 전부 '뒤 코너에서 스매시 뒤 블록을 쫓아 네트로'(3.5~4.3 m) 유형으로 전술상 의도된 이동(BB 스매시 수비: 블록은 상대를 앞으로 끌어낸다). scripts/shot-v18.mjs 19화면 급수/잠금 없음, page errors 없음.
- 단식 4주제 합계 장면 369(96+108+75+90). 남은 것: 앱 제목 결정(사용자), 폰 확인 뒤 비중·문구 조정.

### v1.8.1 (2026-09-24): 리뷰어 지적 반영 — 상대 응수 코스 방향 (wgr_12aeb6a0febdcc13c44ef8d7)
- 지적: 클리어·드롭 랠리 → 빠른 드롭 크로스 → 상대 '리프트 크로스'(cd2_drcr_o_2)가 좌표상 같은 쪽 직선([1.145,-2.19]→[1.5,6.2])인데 '크로스·내 이동 거리 최대'라고 가르침. v1.8 기록의 '코스 방향 불일치 0'은 내 샷만 검사한 결과라 이 오류를 못 잡았음.
- 원인: 드롭 뒤 상대 응수 표(afterDrop)를 절대 좌표로 적어 내 스트레이트 드롭(상대가 왼쪽) 기준으로만 맞았고, 크로스 드롭(상대가 오른쪽) 뒤에는 스트레이트/크로스가 뒤집힘.
- 조치: SGL.oppFollow가 상대 응수에도 dir(st/cr)을 지원(상대 위치 기준). afterDrop 세 갈래를 '네트샷 스트레이트 → 내 앞 / 리프트 스트레이트 → 같은 쪽 뒤 / 리프트 크로스 → 반대 코너 뒤'로 바꿈(라벨의 백핸드/포핸드 표현은 상대 위치에 따라 달라지므로 뺌). 새 검사 scripts/check-course-dir.mjs — 단식 전 주제에서 제목에 스트레이트/직선/크로스가 있는 샷을 나·상대 모두 좌표와 대조(괄호 안 조건문은 제외): 53건 확인, 불일치 0.
- 교훈: '불일치 0' 같은 검사 결과를 적을 때는 검사 범위(누구의 샷, 어떤 장면)를 함께 적는다. 상대 샷도 내 샷과 같은 기준으로 검사한다.


### v1.9 (2026-09-24): 출처 드롭다운 표시 + 단식 추천 코스(3구까지 자동 재생)
- 요청: "출처 문구 옆에 드롭다운 문구 추가 ex) 출처🔻" / "단식에서 서브 측, 리시브 측 3구까지 확률 높은 코스 분석해서 저장하고 추천 코스 버튼 실행시 3구까지 자동 실행해서 볼수있게".
- ① 출처 접기 요약 '출처' → '출처🔻'(요청 예시 그대로 고정. 열림/닫힘에 따라 바꾸지 않음).
- ② 추천 코스 정의: 시작부터 '3구:' 장면에 닿는 경로 가운데 갈래 비중(w ÷ 합)의 곱이 가장 큰 경로. next로 이어지는 장면은 곱 1. 처음 계획한 '단계마다 최대 w'(탐욕)는 리뷰어 지적(wgr_3669bcae7573cc2654c67842)대로 경로 전체 비중을 비교하지 않아 버림 — 서브 측에서 탐욕은 클리어→백핸드 뒤→클리어 스트레이트(2.67%), 곱 최대는 빠른 드롭→백핸드 앞→네트샷 스트레이트(8.73%). 3구에 못 닿는 끝(속임 네트샷·푸시·플릭 서브 뒤 2구 끝, 리시브 측 12개)은 곱이 짧아 유리해지므로 후보에서 제외. 동률이면 데이터에 먼저 적힌 갈래.
- 저장: SGL.likelyPath(N, start)가 빌드 때 계산해 topic.recommended = { ids, share }로 둠(비중을 바꾸면 따라 바뀜. 단식 서브 측·리시브 측만, 주제 3·4와 복식은 없음). 결과 — 서브 측: hs0 > hs1 > hs1_drop > hs2_d_bh > hs3_d_bh > hs3_d_bh_net_0 (8.73%, 3구 경로 55개 중 유일 최대). 리시브 측: rs0 > rs1 > rs1_t > rs2_t > rs2_t_lift > rs2_t_lift_0 > rs2_t_lift_0_o > rs2_t_lift_0_o_0 (3.70%, 48개 중 최대. '상대 클리어 → 내 뒤'와 '상대 빠른 드롭 → 내 앞'이 w 3:3 동률이라 먼저 적힌 클리어 — 비중을 가를지는 사용자 결정 대기).
- 뷰어: footer에 '추천 코스' 버튼(recommended 없는 주제는 숨김, 진행 중엔 '재생 중…'). 누르면 처음부터 다시 시작해 장면 애니메이션이 끝난 뒤 1.4초 쉬고 다음 장면으로(쉬는 동안 busy로 조작 잠금, 다음에 고를 갈래 버튼을 .pick으로 강조), 3구 장면에서 끝. 안내 줄 #autoNote: "재생 중 — 단계마다 보이는 비율(%)을 곱해 3구까지 가장 큰 흐름(추정 n%)" / "끝(3구). 단계별 비율을 곱하면 약 n% — 추정". 탭·'처음' 버튼(gen 증가)으로 중단, 손으로 움직이면 안내를 지움. enter/loadTopic에 완료 콜백 onDone 추가, 갈래 버튼에 data-to.
- 검증: check-data 문제 0(주제 12, 노드 747, 노트 1546, 미검증 0). 새 scripts/check-recommended.mjs(독립 재계산 — 경로가 이어지는지, 3구 끝 장면인지, 최대 곱인지, 동률 알림) 문제 0. check-course-dir 52건 불일치 0. scripts/shot-v19.mjs(390×844): 서브 측 6장면·리시브 측 8장면을 끝까지 자동 재생, 재생 중 '처음'으로 취소 확인, 클리어·드롭 주제에서 버튼 숨김, 출처🔻 눌러 열림(항목 40), page errors 없음. 터널 응답 200 = mobile/index.html 동일.
- 남은 것: 리시브 측 동률(클리어·드롭 w 3:3) 처리, 앱 제목, 비중·문구 조정 — 사용자.

### v2.0 (2026-09-24): 단식 5구까지 + 추천 코스 5구 + 리시브 측 동률 해소 (사용자 "클리어 3 드롭 2", "단식 5구까지 가능하게 변경")
- ① 동률 해소: 리프트 뒤 상대 응수 표(rear) '빠른 드롭 → 내 앞' 비중 3 → 2(클리어 3 · 드롭 2 · 스매시 2). 리시브 측 3구 갈래 6곳에 반영(회귀 검사에서 이 6건만 w 차이).
- ② 5구 확장 범위: 서브 측·리시브 측 두 주제(서브부터 구를 세는 주제). 클리어·드롭 랠리·스매시 수비 주제는 랠리 중간 상황이라 손대지 않음(회귀 검사에서 노드 완전 동일) — 필요하면 사용자 결정.
- 방식(랠리 엔진, SGL.extendRally): 끝 장면을 이어 붙인다. 내 샷 장면 → '상대 N구 예상'(replyFor: 내 샷 종류·낙하점으로 응수 표 선택) → 응수 장면 → '내 N구'(sitFor: 상대 샷 종류·낙하점으로 상황 선택 → 기술 → 코스) → 내 샷 장면 → … 5구 장면에서 끝. 킬로 끝난 랠리(win)는 잇지 않음. 원래 장면은 next가 생기고 end 문구가 다음 화면의 '기본' 줄로 옮겨가는 것 말고 그대로(회귀 검사 112건 전부 이 형태).
- 표 공유: 서브 측의 상황(SIT2)·기술·코스(TECH)와 리시브 측의 리프트·네트샷 뒤 응수(REPLY_TBL)를 SGL 공통으로 올려 3구와 4·5구가 같은 문구·비중을 쓴다. 클리어·드롭·스매시·블록·드라이브 뒤 응수(SGL.REPLY)는 주제 3·4의 표와 같은 출처·비중(BB 클리어·드롭·네트샷·리프트·스매시·스매시 수비, BI 리시브·수비)으로 새로 적음. 엔진용 기술 8개 추가(네트샷 되놓기·네트 뒤 리프트·네트킬·미드 스매시·미드 드롭·미드 블록·드라이브 되받기·리프트 중앙; 근거 없는 것은 추 표시).
- 코스 dir 확장: open(상대가 없는 쪽), body(상대 몸) — 블록·드라이브·몸 스매시 코스를 절대 좌표에서 dir로 바꿈(어느 쪽에서 쳐도 맞도록). 기존 장면 좌표는 그대로이고, hs3_c_mid_smash2_1(중앙 클리어 뒤 몸 스매시)만 낙하점 x -0.3 → -0.2(상대 위치 기준).
- 크기: 노드 747 → 5177(서브 측 96 → 1752, 리시브 측 108 → 2882), 노트 8476. 화면 아래 진행 표시가 '10/1752'처럼 커 보임 — 표시 방식은 사용자 결정 대기.
- 추천 코스: 5구 장면에 닿는 경로 중 갈래 비중 곱 최대. 서브 측(988개 경로 중, 1.23%): 하이 서브 → 상대 빠른 드롭 → 내 백핸드 앞 → 네트샷 스트레이트 → 상대 네트샷 되놓기 → 내 네트샷 되놓기 스트레이트. 리시브 측(1524개 중, 0.47%): 로우 서브 T → 네트샷 타이트 스트레이트 → 상대 네트샷 되놓기 → 내 네트샷 되놓기 → 상대 네트샷 되놓기. 동률 없음. 안내 문구는 rec.shots(5)로 표시.
- 검증: check-data 문제 0(주제 12, 노드 5177, 노트 8476, 미검증 0). 회귀(/tmp/patch/regress.mjs, v1.9 노드 기준): 허용 차이(end→기본 112, 드롭 w 6, 몸 스매시 좌표 2, 킬 win 3) 말고 0, 주제 3·4 동일. check-recommended 문제 0(샷 수로 5구 확인). check-course-dir 871건 불일치 0. 단식 노트 60자 초과 0, 선 밖 0. check-moves 3.5 m 초과 250건(전부 4.5 m 이하: 네트 앞에서 리프트·킬 뒤 베이스 복귀 ≈4.0 m, 상대가 내 네트샷·블록을 쫓는 이동 ≈3.5~3.7 m — 실제 동작 범위). scripts/shot-v20.mjs: 서브 측 10장면·리시브 측 12장면 자동 재생 끝까지(5구), 재생 중 '처음' 취소, 버튼 숨김, 출처🔻, page errors 없음. 터널 = mobile/index.html.
- 파이프라인: cp /tmp/index.v1.5.bak.html → apply_v16.py(singles.js·singles34.js 삽입) → apply_v19.py(뷰어). singles.js에 공통 표·엔진 포함(/tmp/patch/patch_v20.py로 v1.9 판에서 변환, 백업 singles.v19.bak.js).

### v2.1 (2026-09-24): 제목 'Mind Control : Badminton' · 첫 화면 급수 설명 문구 삭제 · 버전만 표시 (사용자 요청 3건)
- 제목: '복식 상황 학습' → 'Mind Control : Badminton' — <title>, 헤더 h1, 첫 화면 h2 세 곳. 헤더는 390px에서 잘림 없음(scrollWidth ≤ clientWidth 확인).
- 첫 화면: '급수는 선택 화면의 비율(%)과 불가 표시… 나중에 헤더의 설정 버튼으로 바꿀 수 있다.' 문단(.start-note) 삭제. 급수 선택 줄은 그대로(단식이면 숨김).
- 버전: DATA.version = 'v2.1'(헤더·첫 화면 같은 값). 설명은 이 문서에만 둔다.
- 데이터·로직 변경 없음: check-data 문제 0(노드 5177), check-recommended 문제 0, page errors 없음, 터널 = mobile/index.html. 파이프라인: … → apply_v19.py → apply_v21.py.
- 앱 제목 결정 대기 항목은 이걸로 닫힘.

### v2.2 (2026-09-24): 오프닝 영상 + 상단·하단 톤앤매너 통일 (사용자 "ui/ux 개선작업 진행 … 첨부 영상은 첫 시작시 오프닝 … 상단 하단 톤앤매너 맞춰서")
- 영상: 사용자 첨부 badminton_no_smash_a1b2c3.mov(480×854, H.264 Baseline 3.0, 24fps, 2.7초, 626KB, 오디오 없음)를 mp4로 리멕스(재인코딩 없음, faststart) → assets/opening.mp4. 단일 파일 유지를 위해 index.html에 base64로 내장(834KB, 파일 1.15MB). 실행 때 Blob URL로 풀어 <video muted playsinline>에 넣는다.
- 오프닝 동작: 페이지를 열면 네이비 바탕 오버레이에서 자동 재생 → 끝나면(ended) 오버레이 제거 → 시작 화면. 화면 탭이나 '건너뛰기'로 바로 넘김. H.264를 못 트는 브라우저(canPlayType ''), 자동 재생 거부(play() 거부), 오류(error), 움직임 줄이기 설정이면 바로 시작 화면. 안전장치 6초. 매번 페이지를 열 때 재생(기억 없음).
- 톤앤매너: 오프닝 포스터 팔레트(하늘색 바탕·네이비 글자·오렌지 강조·노란 그립)를 상단·하단에 적용. 헤더·탭·설정 버튼·코트 바탕·코트 안내·푸터 = 네이비(#14335a) 위 흰 글자, 현재 탭·주요 버튼(다음/선택/시작) = 오렌지(#f07a1a), 추천 코스 버튼 = 노랑 테두리, 코트 안내 글 = 하늘색. 설명 판은 흰색 그대로(읽기용), 제목·갈래 테두리는 네이비. 시작 화면은 하늘색(#bfe0f5) 바탕에 흰 카드·네이비 제목·오렌지 시작 버튼. 코트 색과 선수 표시(나 파랑·상대 빨강)는 기능 요소라 그대로.
- 레이아웃(상단 헤더·탭 / 중간 코트 / 하단 설명·버튼)과 데이터·장면 로직은 변경 없음.
- 검증: check-data 문제 0(노드 5177), check-recommended 문제 0. 헤드리스 크로미엄(H.264 재생 가능): 1.5초 시점 currentTime 1.51·재생 중·480×854, 3.3초에 오버레이 사라지고 시작 화면; src를 막고 play를 성공으로 흉내 낸 페이지에서 '건너뛰기' 누르면 즉시 사라짐, 안전장치는 8.6초 뒤 사라짐 확인(당시 8초 설정, 지금 6초). page errors 없음. 화면 5장(오프닝·시작·본 화면·갈래·추천 코스 재생) 확인, 터널 = mobile/index.html.
- 파이프라인: … → apply_v21.py → apply_v22.py(assets/opening.mp4 읽어 내장).

### v2.3 (2026-09-24): 파랑 톤 채도 낮춤 + 오프닝 재생 보강 (사용자 "파랑톤 채도 좀 낮춰줘 / 오프닝은 색 좋다 / 오프닝 첫화면 접속시 영상안나와")
- 팔레트: 같은 밝기에서 채도만 낮춤 — 네이비 틀 #14335a → #24354c(채도 64→35%), 코트 안내 하늘색 #9ad0f0 → #adcbdc(74→40%), 시작 화면 바탕 #bfe0f5 → #c9dce8(73→40%), 추천 갈래 강조 #e8f3fb → #e9eff4. 오프닝 배경(#0e2542)과 오렌지·노랑은 그대로.
- 영상이 안 나온 원인(가장 유력): 자동 재생이 거부되면(iOS 저전력 모드, 브라우저 정책) v2.2 코드가 조용히 오버레이를 없애 시작 화면으로 갔음 → 사용자는 영상을 못 봄. 고침: ① 포스터(첫 프레임 JPEG 44KB, base64 내장)를 즉시 표시 ② play()가 거부되면 '▶' 버튼을 띄워 탭으로 재생(사용자 동작이라 허용됨) ③ Blob URL 오류면 data URI로 한 번 더 ④ muted·playsInline 속성을 JS로도 명시, autoplay 속성 추가 ⑤ 안전장치는 '재생도 안 되고 ▶ 버튼도 안 떴을 때'만 6초 뒤 시작 화면(▶ 버튼 상태에서는 사라지지 않음) ⑥ 재생 중엔 ended 또는 영상 길이+3초에 시작 화면.
- 검증(헤드리스 크로미엄): (a) 정상 자동 재생 — 1.5초 시점 재생 중, 3.1초에 오버레이 사라지고 시작 화면. (b) 막힌 자동 재생 흉내(src 넣을 때 autoplay 속성 제거 + 첫 play() NotAllowedError) — 포스터와 ▶ 버튼 표시, 7.5초 뒤에도 유지, ▶ 탭하면 재생(1.2초 시점 currentTime 1.21) → 끝나고 시작 화면. (c) 건너뛰기 즉시. page errors 없음. check-data 문제 0, check-recommended 문제 0. 파일 1,214,915B. 터널 = mobile/index.html.
- 헤드리스 검증 메모: 크로미엄은 음소거 영상을 정책과 무관하게 자동 재생하므로 --autoplay-policy로는 막힘을 흉내 못 낸다. addInitScript에서 document.documentElement를 쓰면 그 시점엔 null이라 스크립트가 조용히 죽는다 — 흉내가 적용됐는지 카운터로 확인할 것.
- 파이프라인: … → apply_v22.py → apply_v23.py(assets/opening-poster.jpg 내장).

### v2.4 (2026-09-24): 오프닝 소스 3단 + 실패 원인 표시 (사용자 "영상 실행 안 되고 바로 화면이 넘어 간다 … 새로고침 해도 동일한 증상")
- 증거 영상 관찰(사용자 화면 녹화 8.57초, fps 4·10으로 프레임 추출): 포스터+건너뛰기가 약 0.25초(4fps 한 프레임) 보이고 곧바로 시작 화면. ▶ 버튼은 어느 프레임에도 없음. 이후 8초 동안 시작 화면 그대로.
- 추론(v2.3 코드 경로와 대조, 에디터 지적으로 관찰과 분리): play() 거부 경로는 ▶를 띄우고 그 상태에서는 6초 안전장치도 안 돌아 시작 화면으로 못 간다 → 위 관찰과 안 맞아 제외. 0.25초 안에 finish()를 부르는 경로는 ① error 이벤트 2회(blob 실패 → data URI 실패) ② 움직임 줄이기 설정 ③ canPlayType ''(iOS는 H.264를 트니 가능성 낮음). 오버레이는 본문 끝 스크립트보다 먼저 칠해지므로 ②③도 '잠깐 보였다 사라짐'과 맞는다. 셋 중 무엇인지는 녹화만으로 못 가른다 → 진단 표시를 넣었고, 실제 원인은 사용자 폰 확인 전까지 미확정.
- 구조: <video>에 <source> 3개 — ① assets/opening.mp4(별도 파일, HTTP) ② blob URL ③ data URI(둘 다 내장 base64에서 생성). 브라우저가 앞에서부터 시도하고, 각 source의 error를 모아 마지막 소스까지 실패했을 때만 시작 화면. play() NotAllowedError → ▶. 움직임 줄이기 설정이면 autoplay 속성을 떼고 포스터 + ▶(조용히 건너뛰지 않음). 시작 화면으로 비정상 종료하면 버전 줄에 "오프닝 못 틀음: … (net N ready N err N)"을 붙여 폰 스크린샷으로 원인을 볼 수 있다.
- 서버: iOS Safari는 매체를 Range 요청(206)으로 받는데 python http.server는 Range 미지원(200 전체만) → web/scripts/static-server.mjs(node, 0.0.0.0:8088, Range 206, Cache-Control no-cache)로 교체. 별도 파일은 mobile/assets/opening.mp4(assets/opening.mp4 복사, 625,844B). 단일 파일 배포(file://)에서는 http 소스가 실패하고 blob이 이어받는다.
- 검증(헤드리스 크로미엄, 390×844): (b) HTTP — 요청 206 bytes 0-625843/625844, 1.5초 시점 currentTime 1.52 재생 중, 3.2초에 시작 화면, 버전 줄 "v2.4". (a) http 소스 차단 — blob으로 재생(1.51), 3.0초에 시작 화면. (c) http 차단+blob 생성 실패+data 깨뜨림 — 0.3초에 시작 화면, 버전 줄 "v2.4 · 오프닝 못 틀음: blob 생성 실패 → http 실패 → data 실패 → 소스 전부 실패 (net 3 ready 0)". (d) 자동 재생 거부 흉내 — ▶ 표시, 탭 뒤 0.81 재생, 정상 종료. (e) 움직임 줄이기 — 자동 재생 없이 포스터+▶, 7.5초 지나도 유지, 탭하면 재생 후 정상 종료. page errors 0. check-data 문제 0(노드 5177), check-recommended 문제 0. 터널 = mobile/index.html. 사용자 폰 재생은 미확인(답 대기).
- 파이프라인: … → apply_v23.py → apply_v24.py.
- v2.4.1 서버 보강(리뷰어 wgr_f241836c509c1fce7a7b225e): static-server.mjs가 잘못된 URL 인코딩(/% 등)의 URIError를 안 잡아 프로세스가 죽었다(임시 포트 8089에서 GET /% → 연결 끊김·프로세스 종료로 재현). 요청마다 try/catch(URIError 400, 그 밖 500), 파일 스트림 error 처리, 루트 경계는 '루트/' 접두 비교(형제 디렉터리 이름 겹침 차단). 교체 뒤 확인: /% 400, /%E0%A4%A 400, /../mobile-x 403, / 200(cmp 동일), Range 206, 범위 밖 416, 서버 생존, 터널 200·206.

### v2.5 (2026-09-24): 오프닝 자동 재생 — 움직임 줄이기 분기 제거 + 거부 시 WebP 대체 (사용자 "재생버튼 눌러서 재생되는데 바로 실행되도록 변경")
- 관찰: v2.4에서 사용자 폰에 ▶ 버튼이 떴고 탭하면 재생됨 → 매체 경로(별도 파일 + Range 서버)는 해결. 녹화 프레임의 하단 주소창 = Safari 본체(인앱 브라우저 아님), 상태바 배터리 아이콘 흰색(녹화 시점 저전력 모드 아님).
- 추론(확정 아님): v2.4에서 ▶가 뜨는 경로는 둘 — ① 움직임 줄이기 설정(내가 v2.4에 넣은 분기: 자동 재생 대신 ▶) ② play() 거부(iOS 저전력 모드·자동 재생 설정). ①이면 v2.3(그 설정에서 즉시 finish)의 '0.25초 뒤 시작 화면'과 v2.4의 ▶가 한 원인으로 설명된다. ②는 배제 못 함.
- 고침: 움직임 줄이기 설정은 무시하고 항상 자동 재생 시도(로그에만 남김). play()가 NotAllowedError로 거부되면 <video>를 숨기고 애니메이션 WebP(assets/opening.webp, 480×854, 65프레임 2,707ms, loop 1, 1,705,542B, ffmpeg libwebp_anim q70 level4)를 <img>로 바로 재생 — 이미지는 매체 자동 재생 정책을 안 받는다. 불러오는 동안 ▶도 같이 두고, WebP를 못 불러오면 ▶만 남긴다(탭 재생). WebP는 index.html에 내장하지 않고 assets/에서 필요할 때만 받는다(file:// 단일 파일에서는 ▶ 경로). 대체 경로를 탔으면 버전 줄에 "오프닝 대체 경로: …"를 붙여 폰에서 어느 경로였는지 볼 수 있다. 종료는 OPENING_MS(2708)+400ms.
- 서버: static-server.mjs MIME에 .webp(image/webp) 추가(없으면 application/octet-stream — 브라우저는 스니핑으로 열지만 바로잡음).
- 검증(헤드리스 크로미엄): (1) HTTP 정상 — 1.5초 재생 중, 3.5초 시작 화면, 버전 줄 "v2.5". (2) 자동 재생 거부 흉내 — 0.7초에 video 숨김·img 표시(naturalWidth 480, webp 200), 0.7초와 1.6초 화면이 다름(움직임), 3.8초 시작 화면, 버전 줄 "v2.5 · 오프닝 대체 경로: 자동 재생 거부(NotAllowedError) → WebP 대체 재생 → ended". (3) 거부 + webp 차단 — ▶ 표시, 7초 유지, 탭 뒤 0.80 재생, 버전 줄 "… → WebP 못 불러옴 → ended". (4) 움직임 줄이기 흉내 — 이제 자동 재생(1.51), 3.3초 시작 화면, 표식 없음. (5) 소스 전부 실패 — 진단 줄 그대로. page errors 0. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,217,076B. 터널 = mobile/index.html, 터널 webp 200. 사용자 폰 자동 재생은 미확인(답 대기).
- 파이프라인: … → apply_v24.py → apply_v25.py.

### v2.6 (2026-09-24): 시작 화면 배경 = 사용자 첨부 포스터 (사용자 "첫화면 배경을 첨부한이미지로 변경")
- 이미지: 사용자 첨부 4B742043-….jpg(736×1308 JPEG, 104,048B — 오프닝 포스터와 같은 그림) → assets/start-bg.jpg. 단일 파일 유지를 위해 base64(138,732자)로 CSS에 내장.
- 적용: .start 배경 var(--sky2) → var(--navy2) + url(data:image/jpeg;base64,…) center / cover no-repeat. 흰 카드·제목·버튼은 그대로(가독성). 다른 화면은 변경 없음.
- 검증(헤드리스 크로미엄): 390×844·375×667에서 getComputedStyle(#start) backgroundImage = data:image/jpeg, backgroundSize cover, position 50% 50%, 버전 줄 "v2.6", page errors 0. 스크린샷으로 카드 뒤 포스터 확인. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,355,934B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v25.py → apply_v26.py(assets/start-bg.jpg 내장).

### v2.7 (2026-09-24): 게임 화면 상단·하단 배경색 = 사용자 첨부 색 견본 (사용자 "게임시작중에 상단, 하단 색상 첨부이미지 색스타일로 색변경")
- 첨부: 8C4C629D-….jpg는 37×67 단색 견본(assets/tone-ref.jpg). 픽셀 평균 #334564(위쪽 띠 #354766 → 아래쪽 #2f4260로 아주 약한 차이, 양자화 상위 색 #314361 54%). 단일 색 #334564로 채택(차이가 눈에 안 띄어 그라데이션은 안 함).
- 적용: 새 변수 --chrome: #334564. header(탭 포함)·.court-wrap·.court-note·footer 배경만 var(--navy) → var(--chrome). 상단은 헤더부터 코트·코트 안내까지 한 덩어리라 코트 바탕도 포함. --navy(#24354c)는 글자·갈래 테두리·시작 화면 제목·선택 버튼에 그대로. 오렌지 버튼·노랑 추천 코스 테두리·시작 화면 배경(포스터)·오프닝 변경 없음.
- 검증(헤드리스 390×844): getComputedStyle 배경 header/court-wrap/court-note/footer 모두 rgb(51,69,100), .start-card h2 색 rgb(36,53,76) 그대로, page errors 0. 스크린샷(첫 장면·갈래 장면·시작 화면) 확인. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,356,084B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v26.py → apply_v27.py.

### v2.8 (2026-09-24): 시작 화면 카드 반투명 (사용자 "흰색 배경 불투명으로 해줘 뒷배경 보이게" + 시작 화면 스크린샷)
- 해석: 첨부 스크린샷은 v2.7 시작 화면(흰 카드가 포스터를 가림). "불투명"은 문맥상 반투명(뒷배경 보이게) 의도로 처리.
- 적용: .start-card 배경 #fff → rgba(255,255,255,.75) + -webkit-backdrop-filter/backdrop-filter: blur(4px)(글자 가독성). 카드 안 종목·급수 버튼(#fff)·시작 버튼·다른 화면은 그대로.
- 검증(헤드리스 390×844): getComputedStyle(.start-card) backgroundColor rgba(255, 255, 255, 0.75), backdropFilter blur(4px), 버전 줄 "v2.8", page errors 0. 스크린샷: 카드 뒤로 라켓·글자 비침, 제목·버튼 글자 읽힘. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,356,256B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v27.py → apply_v28.py.
- v2.8 후기(2026-09-24): 사용자 "더 투명하게해줘"로 v2.9(흰색 55%, /tmp/patch/apply_v29.py)를 빌드해 터널에 올렸으나, 곧바로 "아니다 투명하게 안해도된더 / 지금 딱좋아"로 철회. 철회 문구가 결과를 보기 전의 취소로 읽혀 v2.8(75%)로 되돌림(파이프라인은 apply_v28까지). 사용자가 55%를 보고 한 말이었으면 apply_v29를 다시 얹는다. 같은 답에서 "오프닝 자동 재생 정상임" 확인 → v2.3부터 이어진 오프닝 미재생 건 종료(실제 원인은 움직임 줄이기 분기/재생 거부 중 미확정, 두 경로 다 처리됨).

### v2.10 (2026-09-24): 게임 화면 하단 영역 반투명 + 포스터 배경 (사용자 "게임네 하단영역도 투명화해줘 배경")
- 해석: "도" = 시작 카드(v2.8)처럼 포스터가 비치게. 게임 화면 body 배경에 포스터(cover, 뷰포트 크기 — body는 100dvh라 fixed 불필요), 하단 영역 = 설명 패널(.panel: rgba(255,255,255,.75) + blur 4px) + 푸터(rgba(51,69,100,.75) = --chrome 75% + blur 4px). 상단 헤더·탭·코트 바탕·코트 안내는 불투명 --chrome 그대로(요청이 하단만). 패널 안 갈래 버튼·설명 줄은 그대로.
- 포스터 data URI는 :root --poster 한 곳에 두고 .start와 body가 var(--poster)로 공유 → 내장 1회(파일 1,356,709B, v2.8 대비 +453B).
- 검증(헤드리스 390×844): 시작 화면 #start backgroundImage data:image/jpeg(변수 경유), 카드 rgba .75 그대로; 게임 화면 body backgroundImage data:image/jpeg·cover, header/court-wrap rgb(51,69,100) 불투명 유지, .panel rgba(255,255,255,0.75)+blur(4px), footer rgba(51,69,100,0.75)+blur(4px), page errors 0. 스크린샷(첫 장면·갈래 장면): 설명 영역 뒤로 포스터의 라켓 그립이 흐릿하게 비침, 글자 읽힘. check-data 문제 0(노드 5177), check-recommended 문제 0. 터널 = mobile/index.html.
- 파이프라인: … → apply_v28.py → apply_v210.py (apply_v29.py는 철회, 미적용).

### v2.11 (2026-09-24): 푸터 다시 불투명, 내용 영역은 반투명 유지 (사용자 "맨하단 이전,처음,다음 버튼 영역 불투명화해줘. 그 위에 내용 영역은 흰색 그대로 두고")
- 해석: 직전 요청에서 "투명화"를 바로 썼으므로 이번 "불투명화"는 반대 뜻(단색). v2.10에서 패널·푸터를 함께 반투명으로 한 것 중 푸터만 되돌리고 패널(흰색 75% + 블러)은 그대로. 해석이 틀리면(푸터를 더 비치게) 한 줄로 바꿀 수 있다고 응답에 명시.
- 적용: footer 배경 rgba(51,69,100,.75)+blur → var(--chrome) 단색. 패널·body 포스터·상단 변경 없음.
- 검증(헤드리스 390×844): footer rgb(51,69,100)·backdropFilter none, .panel rgba(255,255,255,0.75)+blur(4px) 유지, body 포스터 유지, 버전 줄 "v2.11", page errors 0. 스크린샷 확인. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,356,693B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v210.py → apply_v211.py.

### v2.12 (2026-09-24): 푸터 반투명(비침) + 내용 영역 흰색 단색 — v2.11 뒤집음 (사용자 "버튼영역의 배경을 불투명화 해. 내용 영역 흰색이고 불투명화 해제")
- 용어 해석(추정, 사용자 확인 없음): 이번 요청은 문맥상 "불투명화" = 비치게(반투명), "불투명화 해제" = 단색으로 읽었다. v2.11(푸터 단색·패널 반투명)은 반대로 읽은 것 → 정정. 정의를 사용자가 확인한 것은 아니므로 관찰로만 메모리(user-word-usage)에 기록(에디터 지적 반영).
- 적용: footer rgba(51,69,100,.5) + blur 4px(--chrome 50% — 흰 글자 대비: 포스터 하단 연파랑 위에서 약 3.8:1, 16px 굵은 글자 기준 충분). .panel #fff 단색, 블러 제거. body 포스터·상단·시작 화면 변경 없음.
- 검증(헤드리스 390×844): .panel rgb(255,255,255)·backdropFilter none, footer rgba(51,69,100,0.5)·blur(4px), body 포스터 유지, 버전 줄 "v2.12", page errors 0. 스크린샷(첫 장면·갈래·푸터 확대) 확인: 버튼 줄 뒤로 포스터 연파랑 비침, 버튼 글자 읽힘. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,356,751B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v211.py → apply_v212.py.

### v2.13 (2026-09-24): 푸터 뒤 포스터 위치 조정 (사용자 "배경애 투명화 70% 적용 안됐어" + v2.12 스크린샷)
- 판독: 첫 스크린샷(푸터만, 버전 없음)은 푸터 색 #374563 = v2.11 단색과 일치 → 이전 판 화면 가설. 두 번째 스크린샷은 헤더 v2.12, 푸터 색 #6d88a1 = 50% 슬레이트 + 포스터 연파랑(기대 #6987a0) → 반투명은 적용돼 있었음. 단색처럼 보인 이유 = cover 배치에서 푸터 뒤 포스터 부분이 민무늬 연파랑. 70%로 올려도 더 밝아질 뿐 그림은 안 보임.
- 적용: body 포스터 background-size cover → auto 200%, position center → center 3%. 게임 화면에서 포스터가 비치는 곳은 푸터뿐이라 다른 영역엔 영향 없음(헤더·코트·패널 불투명). 390×844·414×896에서 푸터 뒤 = 원본 y≈600~680(셔틀·라켓 교차·흰 띠). 푸터 50%+블러 유지, 시작 화면은 별도 배경(center/cover)이라 그대로.
- 검증(헤드리스): 390×844·414×896 computed body backgroundSize 'auto 200%'·position '50% 3%', footer rgba(51,69,100,0.5), 버전 줄 "v2.13", page errors 0. 푸터 확대 스크린샷: 셔틀 코르크(주황)·라켓 손잡이·흰 띠가 흐릿하게 비침, 버튼 글자 읽힘. 시작 화면 스크린샷 변화 없음. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,356,804B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v212.py → apply_v213.py.

### v2.14 (2026-09-24): 버튼 줄을 본문 위에 겹침 — 반투명 뒤로 본문이 비침 (사용자 "드디어 적용 확인했어, 그런데 본문 내용이 나왔으면 좋겠어 지금은 배경이 나온다")
- 사용자 확인: v2.13 푸터 반투명·포스터 비침 적용 확인("드디어 적용 확인했어"). 새 요청 = 뒤에 포스터가 아니라 본문(설명 영역)이 비치게.
- 적용: body position relative, footer position absolute·left/right/bottom 0·z-index 5(시작 화면 10·오프닝 20 아래). .panel padding-bottom 12px → calc(72px + env(safe-area-inset-bottom))(푸터 64px + 여유)로 마지막 내용이 푸터 위로 스크롤됨. footer button 배경 transparent → rgba(51,69,100,.55): 흰 본문 위 50% 슬레이트 바는 흰 글자 대비 약 2.3:1이라 버튼만 진하게(버튼 위 약 5:1). 푸터 바 50%+블러·주요 버튼 오렌지·추천 코스 노랑 그대로. 게임 화면에서는 이제 포스터가 안 보임(v2.13 위치 조정은 남겨 둠, 영향 없음). 시작 화면 그대로.
- 검증(헤드리스 390×844): footer top 780·bottom 844, .panel bottom 844(푸터 아래까지 확장), padding-bottom 72px, position absolute, elementFromPoint(푸터 위쪽) = 푸터 버튼, #btnPrev 배경 rgba(51,69,100,0.55), #btnNext 오렌지. 끝까지 스크롤 시 panel scrollTop 146/scrollHeight 449, 갈래 장면 마지막 .branch bottom 732 < footer top 780(버튼 위로 올라옴). page errors 0. 스크린샷: 버튼 줄 뒤로 본문 글이 흐릿하게 비침, 버튼 글자 읽힘. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,357,123B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v213.py → apply_v214.py.

### v2.15 (2026-09-24): 버튼 줄 바 배경 완전 투명 (사용자 "아예 배경 투명도 100 하자")
- 적용: footer 배경 rgba(51,69,100,.5)+블러 → transparent, 블러 제거, pointer-events none(바 영역의 빈 곳을 누르면 본문이 반응). footer button 배경 .55 → .78(바가 사라져 버튼만으로 v2.14와 같은 진하기 → 흰 글자 대비 유지) + pointer-events auto. 오렌지 주요 버튼·본문 아래 여백(72px+안전영역)·시작 화면 변경 없음. 비활성 버튼(opacity .35)은 본문 위에서 옅게 보임 — 비활성 표시라 그대로.
- 검증(헤드리스 390×844): footer backgroundColor rgba(0,0,0,0)·backdropFilter none·pointerEvents none, #btnPrev 배경 rgba(51,69,100,0.78)·pointerEvents auto, elementFromPoint(버튼 사이 틈) = 본문 LI, elementFromPoint(버튼 위) = BUTTON#btnRestart, 다음 버튼 클릭 1/162 → 2/162, 버전 줄 "v2.15", page errors 0. 스크린샷: 바 없이 버튼만 본문 위. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,357,112B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v214.py → apply_v215.py.

### v2.16 (2026-09-24): 버튼 줄 바 투명도 80 = 슬레이트 20% (사용자 "배경없으니 안되겠네, 배경 투명도 80")
- 척도: 직전 "투명도 100" = 완전 투명이었으므로 투명도 80 = alpha .2. footer 배경 transparent → rgba(51,69,100,.2) + 블러 4px 복원. 바가 다시 보이므로 터치 통과(pointer-events none)는 끔(바 아래로 지나가는 갈래 버튼 오터치 방지, v2.14와 같음). 버튼 .78 유지.
- 검증(헤드리스 390×844): footer rgba(51,69,100,0.2)·blur(4px)·pointerEvents auto(버튼 사이 = FOOTER), #btnPrev rgba .78, 다음 클릭 1/162 → 2/162, 버전 "v2.16", page errors 0. 스크린샷: 옅은 바 + 블러, 버튼 글자 읽힘. check-data 문제 0, check-recommended 문제 0. 파일 1,357,148B.
- 파이프라인: … → apply_v215.py → apply_v216.py.

### v2.17 (2026-09-24): 코트 아래 안내 문구 삭제 (사용자 "문구 삭제해서 아래 내용을 더 볼수있게 해줘 내용영역이 너무 작아서 확보 필요")
- 삭제 대상: 코트 아래 한 줄(#courtNote) — 단식 서브/랠리, 복식 서브/랠리 네 문구("단식 서브: 칠한 옆줄은 아웃, 뒤는 끝선까지 인." 등). .court-note CSS·마크업·DOM 쓰기 한 줄 제거, 문구 계산(text)은 칠한 구역과 짝이라 주석과 함께 남김. 코트의 칠한 구역(아웃 표시)과 코트 높이(max-height 50vh)는 그대로(요청 밖).
- 검증(헤드리스 390×844): #courtNote 없음, 코트 바닥 515 = 패널 위 515, 패널 clientHeight 303 → 329(+26px), 푸터 위로 보이는 본문 265px, 단식·복식 모두 page errors 0, 버전 "v2.17". check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,357,089B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v216.py → apply_v217.py.

### v2.18 (2026-09-24): 코트 높이 50vh → 42vh, 본문 위로 (사용자 "코트 부분 영역과 내용 비율을 좀 조절해줘. 내용영역을 좀더 위로")
- 코트(svg#court)는 max-height가 세로 기준이라 이 값이 코트:본문 비율을 정한다. "좀" = 42vh(약 16% 축소). 헤더·탭·본문 스타일·푸터 변경 없음.
- 검증(헤드리스): 390×844 — 코트 높이 422 → 354px, 본문 시작 515 → 447px(68px 위로), 본문 clientHeight 329 → 397px, 푸터 위로 보이는 본문 333px. 414×896 — 코트 376px, 본문 시작 469px, clientHeight 427px. page errors 0, 버전 "v2.18". 스크린샷: 코트 선수 이름표 여전히 읽힘. check-data 문제 0(노드 5177), check-recommended 문제 0. 파일 1,357,177B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v217.py → apply_v218.py. 비율은 이 한 줄(42vh)로 조정.

### v2.19~v2.20 (2026-09-24): 갈래(선택 박스) 목록 2열 배치 + 판박이 꼬리말 제거 (사용자 "선택 박스들 하위 리스트인데 한줄에 두개씩 배열해도 될거같아. 너무 글자가 길면 좀 줄여서 불필요한 내용 빼고." + 스크린샷)
- 스크린샷 판독: "상대가 무엇으로 받나?" 갈래 4개(클리어→코스 선택 37%, 빠른 드롭→코스 선택 36%, 스매시→코스 선택 18%, 스트레이트 슬라이스(앞쪽에 짧게) 9%) — 세로 한 줄씩이라 스크롤이 길다.
- v2.19: `.branches`를 `flex column` → `grid 2열`(`.prompt`는 `grid-column: 1/-1`로 두 칸 다 차지). 전체 4265개 갈래 라벨 중 384개가 끝에 판박이 꼬리말 "→ 코스 선택"이 붙어 있었다(모든 선택지가 어차피 코스 선택으로 이어지는 노드라 의미 중복) — 화면 표시(`renderBranches`의 `lbl.textContent`)에서만 이 꼬리말을 정규식으로 뗀다. **데이터의 `b.label` 자체는 그대로**라 회귀 비교·추천 코스 재생·`markCourses`가 쓰는 "(…백핸드)" 꼬리표(끝나는 패턴이 달라 겹치지 않음)에는 영향 없다. 갈래 개수가 홀수면 마지막 하나에 `.wide`(두 칸 전체) 클래스를 붙여 빈 칸을 없앤다.
- v2.20: 좁아진 칸에서 "코너"→"코"+"너"처럼 음절 중간에서 줄바꿈되던 것을 `.branch .lbl { word-break: keep-all; overflow-wrap: break-word; }`로 고쳐 단어(공백) 단위로만 줄바꿈.
- 의도적으로 안 건드린 것: 괄호 설명(예: "(앞쪽에 짧게)", "(P 백핸드)")은 실제 전략 정보라 임의로 안 뺐다. 이런 라벨은 2열에서도 여전히 2~3줄로 접힌다(내용 손실 없이 줄바꿈만 됨) — 사용자가 원하면 개별적으로 더 줄인다.
- 검증: 전체 4265개 갈래 라벨 길이 분포(median 14, p90 22, max 48) 확인, 꼬리말 제거 후 384개 길이(median 3, max 26)로 대폭 감소. 헤드리스 390×844: 단식 진입 시 첫 탭 「하이 서브 · 서브 측」(singles-high-serve)의 1번째 갈래 장면("1 상대 리턴 종류" — v1.6 문서의 클리어4·빠른드롭4·스매시2·슬라이스1)에서 `.branches` computed display grid·2열 177px, 라벨에 "코스 선택" 잔존 없음("클리어","빠른 드롭","스매시","스트레이트 슬라이스 (앞쪽에 짧게)" — 비율·문구가 사용자 스크린샷과 일치), 다음 장면(3개, 홀수)에서 마지막 버튼 `.wide` 확인(정정: 처음에 "singles-clear-drop 주제"라 잘못 적었음 — 실제 실행 로그의 활성 탭은 「하이 서브 · 서브 측」였다. 렌더링 코드(`renderBranches`/CSS)는 모든 주제가 공유해 이 확인으로 다른 주제도 같이 검증된다). check-data 문제 0(노드 5177), check-recommended 문제 0, regress.mjs 허용된 차이만(신규 차이 없음), check-course-dir 871건 불일치 0, note-stats 형식 문제 0. page errors 0. 파일 1,357,937B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v218.py → apply_v219.py → apply_v220.py.

### v2.21 (2026-09-24): 갈래 버튼 한 줄 고정 + 말줄임 (사용자 "선택박스 크기 커지는거 싫어 안예뻐 글자를 줄이거나 빼서 기본크기에 맞춰")
- v2.20까지는 긴 라벨이 단어 단위로 줄바꿈돼 버튼이 2~3줄로 커졌다(예: "스트레이트 슬라이스 (앞쪽에 짧게)"). 사용자가 "글자를 줄이거나 빼서"라고 명시해, 앞서 보류했던 괄호 설명 잘라내기를 이번엔 진행한다 — 단 텍스트를 손으로 다시 쓰는 대신 CSS 말줄임(ellipsis)으로 기계적으로 처리(모든 라벨에 똑같이 적용, 사람이 어떤 말이 "중요한지" 판단할 필요 없음).
- 적용: `.branch`에 `min-width: 0`(그리드 칸 안에서 내용 크기만큼 안 넓어지게), 폰트 14→13px(잘리는 빈도를 조금 줄임). `.branch .lbl`은 v2.20의 word-break 방식(줄바꿈) → `white-space: nowrap; overflow: hidden; text-overflow: ellipsis; min-width: 0`(한 줄 고정 + 넘치면 "…"). `min-width: 0`을 버튼(그리드 아이템)과 라벨(플렉스 아이템) 둘 다에 줘야 실제로 잘린다(안 주면 내용 크기만큼 칸이 밀려 넘침) — 흔한 함정이라 둘 다 넣음. 데이터(`b.label`)는 안 건드림 — 화면에 잘려 보일 뿐 버튼을 눌렀을 때의 동작·`data-to`는 그대로.
- 검증(헤드리스 390×844): 4개짜리 장면(하이 서브·서브 측 2/1752) — 버튼 높이 [44,44,44,44](균일), "스트레이트 슬라이스 (앞쪽에 짧게)" → 화면엔 "스트레이트 슬라…"로 잘림(라벨 DOM textContent는 원본 그대로 유지 — 스크린리더 등엔 전체 문구 남음), `.lbl` computed whiteSpace nowrap·textOverflow ellipsis·minWidth 0, 버튼 font-size 13px. 3개짜리 홀수 장면 — 높이 [44,44,44] 균일, 너비 [177,177,362](마지막 `.wide`가 두 칸 폭). check-data 문제 0(노드 5177), check-recommended 문제 0, check-course-dir 871건 불일치 0, note-stats 형식 문제 0, regress.mjs 기존 허용 차이만(신규 차이 0). page errors 0. 파일 1,358,119B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v220.py → apply_v221.py.

### v2.22 (2026-09-24): 말줄임이 기술명 자체를 잘라먹는 문제 수정 — 부가 설명부터 떼고, 그래도 길면 2줄까지 (에디터 지적: "사용자는 문구를 줄이거나 불필요한 내용을 빼라고 했는데, 예시는 '스트레이트 슬라…'처럼 기술명까지 잘렸습니다")
- 원인: v2.21의 `text-overflow: ellipsis`는 화면에 보이는 글자 수만 기준으로 자르는 기계적 처리라 "스트레이트 슬라이스 (앞쪽에 짧게)"에서 핵심 기술명("스트레이트 슬라이스")과 부가 설명("(앞쪽에 짧게)")을 구분하지 못하고 핵심 이름 중간(…슬라이스의 "이스")까지 잘랐다. 사용자가 원한 "불필요한 내용 빼기"는 부가 설명을 빼라는 뜻이었지 이름을 잘라도 된다는 뜻이 아니었다(에디터 지적으로 구분 확인).
- 적용: ① 화면 표시 라벨 생성 로직(`renderBranches`)에 v2.19의 "→ 코스 선택" 꼬리말 제거 뒤 "핵심 — 부가 설명" 분리 단계 추가 — 첫 " (" / " — " / " – " / " - "(앞뒤 공백 있는 대시만) 앞까지만 남긴다. 붙어 있는 하이픈("1-1", "D-2b" 같은 코스 ID)이나 의미 있는 "→"(예: "빠른 드롭 → 내 앞")는 공백 없는 패턴이라 그대로 안 건드림 — 여전히 데이터(`b.label`)는 안 건드리고 화면 표시(`lbl.textContent`)만 바뀐다. ② `.branch .lbl` CSS를 한 줄 말줄임(`nowrap`+`ellipsis`) → 최대 2줄 클램프(`-webkit-line-clamp: 2`, 넘치면 그때만 "…", `word-break: keep-all`로 음절 안 끊음)로 교체 — 부가 설명을 뗀 핵심 이름은 이제 거의 다 1줄에 들어가고, 그래도 긴 이름(예: "1-1 스트레이트 트램라인")은 잘리지 않고 2줄로 펼쳐진다.
- 검증(전체 4265개 갈래 라벨 대상 드라이런, Node vm으로 빌드 파일의 DATA 로드): 길이 분포 median 14→8(부가 설명 제거 효과), 2,239개 라벨이 실제로 짧아짐(부가 설명 있었던 것). 핵심 추출 후에도 9자 넘는 라벨 약 15개(최대 "D-3b 소프트 드롭 후 전진, 서버는 뒤로" 24자) — 이런 것만 2줄 클램프에 걸린다. 표본 확인: "스트레이트 슬라이스 (앞쪽에 짧게)"→"스트레이트 슬라이스"(전체 보존, 에디터 지적 사례 해결), "1 푸시 (평평하게 밀기) → 코스 선택"→"1 푸시", "클리어 — 대부분 스트레이트"→"클리어", "1-1 스트레이트 트램라인 (기본, 화면 오른쪽) (서버 파트너 백핸드)"→"1-1 스트레이트 트램라인"(붙은 하이픈 보존, 괄호 둘 다 제거), "빠른 드롭 → 내 앞"→변화 없음(화살표 안 건드림), "내 백핸드 뒤 코너"→변화 없음(뗄 것 없음).
- 검증(헤드리스 390×844, 하이 서브·서브 측): 4개짜리 장면 — 라벨 ["클리어","빠른 드롭","스매시","스트레이트 슬라이스"] 전부 온전(잘림 없음), 버튼 높이 [44,44,56,56](1줄 44px·2줄이 필요했던 "스트레이트 슬라이스"만 56px로 커짐 — 그리드라 짝인 "스매시"도 같은 행이라 56px), `.lbl.scrollHeight > clientHeight` 전부 false(클램프 밖으로 넘치는 글자 없음 확인). 3개짜리 홀수 장면 — ["내 백핸드 뒤 코너","내 포핸드 뒤 코너","중앙 깊게"] 높이 [44,44,44](변화 없는 라벨은 여전히 1줄), `.wide` 정상. page errors 0. check-data 문제 0(노드 5177), check-recommended 문제 0, check-course-dir 871건 불일치 0, note-stats 형식 문제 0, regress.mjs 기존 허용 차이만(신규 차이 0). 파일 1,358,507B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v221.py → apply_v222.py.

### v2.23 (2026-09-24): 리뷰어 지적 2건 반영 — 44px 균일 복귀 + "핵심 추출만으로는 부족" 사실 확인 (reviewId=wgr_a757a020095c4acbf8285195, wgr_9881d6e68c33fc7c7853306e)
- **지적 1(크기 재발):** v2.22의 2줄 클램프가 버튼 높이를 [44,44,56,56]으로 벌려, 사용자가 v2.21에서 명시한 "선택박스 크기 커지는거 싫어 … 기본크기에 맞춰"를 재위반했다는 지적. 타당함 — CSS를 v2.21과 같은 1줄 고정(`white-space: nowrap; text-overflow: ellipsis`, line-clamp 제거)으로 되돌리고, 핵심 추출(부가 설명 제거) 로직만 남겼다.
- **지적 2(근거 과장):** v2.22 완료 근거의 "9자 넘는 라벨 약 15개만 2줄 클램프에 걸림"이 실측이 아니라 `slice(0,15)` 예시 출력이었다는 지적. 타당함 — 전체를 다시 정확히 셌다.
- **재검증 중 발견(자체, 리뷰어 지적 2를 계기로): 핵심 추출만으로는 부족하다.** 처음엔 임시 `.branches` div를 새로 만들어 측정해 "225개 고유 라벨 중 13개(5.8%)만 초과"로 나왔으나, 이 임시 div는 실제 2열 그리드 폭(부모 `.panel` 안에서 177px, 텍스트 영역 95px)을 못 받고 화면 전체 폭 기준으로 렌더돼 폭을 과대평가했다(방법론 오류 — 실측은 반드시 실제 렌더 중인 `.branches` 안의 진짜 슬롯에서 해야 함). 실제 슬롯에서 재측정하니 **225개 고유 라벨 중 73개(32.4%)**가 44px·1줄에 못 들어가 "…"로 잘린다 — "스트레이트 슬라이스"(10자, 필요 폭 111px vs 실제 95px)조차 "스트레이트 슬라…"로 잘림(스크린샷 확인). 즉 부가 설명 제거로는 core 자체가 10자 넘는 다수 사례(코스 라벨 "A-1/B-2/D-3b…", 문장형 라벨 "가/나/다 …", 화살표 포함 전략 라벨 "리프트 → 남자 공격" 등)를 못 구한다.
  - **정정(v2.24 재검증에서 반영, reviewId=wgr_84a17df3f6dca439f733fed3): 이 시점의 "실제 갈래 4265건 기준 1,365건(32.0%)"은 부정확한 값이었다.** 이 수치는 225개 고유 라벨의 잘림 여부(95px 슬롯 기준)를 그대로 4265개 인스턴스 전체에 곱해 나온 것인데, 실제로는 홀수 개 갈래의 마지막 버튼(`.wide`, 그리드 두 칸을 다 차지 = 280px 폭, 전체 갈래의 679건·15.9%)과 `node.noProb` 노드(pct 배지가 없어 폭이 살짝 다름, 4건·0.09%)는 95px보다 넓은 다른 폭에서 렌더된다. wide 버튼(280px)에서 같은 225개 라벨을 다시 재보면 **0개**가 넘친다(폭이 넉넉해 전부 들어감). 이걸 반영해 정확히 재계산하면 v2.23의 실제 잘림은 4265건 중 **1,242건(29.1%)**이었다(1,365건·32.0%가 아니라).
- **트레이드오프 확인(폰트 축소는 답이 아님):** 10자를 95px에 맞추려면 폰트 13→11px, 최장 24자("D-3b 소프트 드롭 후 전진, 서버는 뒤로")까지 맞추려면 6px대까지 줄여야 해 모바일 가독성 기준 밖. 즉 "44px 균일 유지"와 "이름이 안 잘리게"는 같은 화면 폭 안에서 동시에 못 만족한다 — 사용자 확인 필요(본문 답변에서 질문).
- 적용(코드): `.branch .lbl`을 `display:-webkit-box; -webkit-line-clamp:2`(v2.22) → `white-space: nowrap; overflow: hidden; text-overflow: ellipsis`(v2.21과 동일, 1줄 고정)로 되돌림. 핵심 추출 로직(`renderBranches`의 "→ 코스 선택" 제거 + 부가 설명 분리)은 그대로 유지 — 부가 설명이 있던 라벨(225개 중 155개, 4265건 중 다수)은 여전히 짧아진 효과를 본다.
- 검증: 전체 클린 리빌드(v1.5 베이스 → apply_v16~v223.py 전부 순서대로) 후 check-data 문제 0(노드 5177), check-recommended 문제 0, check-course-dir 871건 불일치 0, note-stats 형식 문제 0, regress.mjs 기존 허용 차이만(신규 0). 헤드리스(390×844, 하이 서브·서브 측 2/1752): 버튼 높이 [44,44,44,44] 균일 복귀(리뷰어 지적 1 해소), "스트레이트 슬라이스" 라벨은 화면에서 "스트레이트 슬라…"로 잘림(핵심 추출 후에도 95px 슬롯 초과 — 위 32% 사례 중 하나, 실측대로 재현 확인). page errors 0. 파일 1,358,496B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v222.py → apply_v223.py.

### v2.24 (2026-09-24): 라벨 문구 자체를 줄임 — "이미 승인된 요청 재확인 없이 실행" (에디터 지적: "사용자는 이미 '글자를 줄이거나 빼서 기본 크기에 맞춰' 달라고 했습니다. … 다시 선택받는 것으로 보입니다")
- v2.23 보고에서 "남은 32% 잘림은 데이터 라벨 재작성이 필요하고 의미 손상 위험이 있어 임의로 안 함, 사용자 승인 대기"라고 한 것에 대한 지적. 타당함 — "글자를 줄이거나 빼서"는 v2.21에서 이미 받은 지시이고, 그 지시의 자연스러운 범위(의미를 지키며 축약)를 넘지 않는 한 다시 승인받을 이유가 없다. 재승인을 구하는 대신 실제로 축약하고 결과를 투명하게 보고하는 쪽으로 바꿨다.
- **원칙(의미 손실 없이 줄일 수 있는 것만 골랐다):**
  1. **어순 재배치(정보 손실 0)** — "핵심 (부가설명)" 순서가 아니라 "부가설명 (핵심)"으로 데이터가 쓰여 있던 라벨은, 핵심 추출 로직이 앞부분을 부가설명으로 오인해 정작 핵심을 놓치고 있었다. 순서만 앞뒤로 바꾸면(원문 텍스트는 그대로, 위치만 이동) 기존 로직이 의도대로 핵심만 뽑는다.
     - `D-3a 위에서 눌러 치기 (스매시)` → `D-3a 스매시 (위에서 눌러 치기)` — 화면: "D-3a 위에서 눌러 치기"(11자, 넘침) → "D-3a 스매시"(6자, 안 넘침).
     - `D-3b 소프트 드롭 후 전진, 서버는 뒤로 (교대)` → `D-3b 소프트 드롭 (후 전진, 서버는 뒤로 교대)` (실행 시 markCourses가 끝에 "(리시버 파트너 백핸드)"를 붙이는 건 그대로) — 화면: "D-3b 소프트 드롭"(9자).
     - `B-2 후위(서버 파트너) 쪽 — …` → `B-2 후위 쪽 (서버 파트너) — …`, `2-2 후위(서버 파트너) 쪽 — …` → `2-2 후위 쪽 (서버 파트너) — …` (markCourses가 끝에 "(서버 파트너 백핸드)"를 붙이는 것도 그대로) — 화면: "B-2 후위 쪽"(8자).
  2. **문맥과 중복되는 조사·부사 제거(정보 손실 없음, 뜻 그대로)** — 화살표(→)가 있는 라벨은 앞뒤를 core-extraction이 안 건드리므로(의도적으로 방향 정보 보존), 이미 상황 설명에 있는 위치·전제 정보를 라벨에서만 덜어냈다.
     - `네트샷/짧은 응수 → 여자가 전위에서 처리` → `네트샷/짧은 응수 → 여자 처리` ("전위에서" 삭제 — 여자가 전위라는 건 이미 그 주제 상황 설명 첫 줄에 고정).
     - `숏서브 → 리턴 후 네트에 남기` → `숏서브 → 네트에 남기`, `숏서브 → 리턴 후 남자 뒤로, 여자 앞으로` → `숏서브 → 남자 뒤로, 여자 앞으로` ("리턴 후" 삭제 — 리턴은 항상 서브 받은 다음이라 당연한 전제).
     - `롱 서브 → 여자 본인이 뒤로 가서 공격` → `롱 서브 → 여자가 뒤로 공격`, `롱 서브 → 남자가 뒤로 가서 공격` → `롱 서브 → 남자가 뒤로 공격` ("본인이"·"가서" 삭제 — 주어 중복·이동 경위 생략, 최종 위치+행동만 남김).
  3. **화면 로직 버그 수정** — "…→ 코스 선택 (부가설명)"처럼 꼬리말 뒤에 괄호가 더 붙으면, "→ 코스 선택" 제거 정규식이 문자열 끝(`$`)에서만 매치되도록 짜여 있어 실패하고(예: "스매시 → 코스 선택 (포핸드라 자주)"가 "스매시 → 코스 선택"까지만 잘려 꼬리말이 화면에 남았음), 구분자 컷 뒤에 꼬리말 제거를 한 번 더 적용하도록 고쳤다. `renderBranches`만 수정, 데이터는 그대로.
  4. **폰트 13→12px** — 버튼 크기(44px)·패딩은 그대로, 텍스트 표시 영역만 넓힌 무해한 조정.
- **의도적으로 그대로 둔 것(축약하면 학습에 필요한 정보나 식별력이 사라질 위험이 있다고 판단한 51개 고유 라벨, 12px 기준):**
  - 코드+코스명형(`A-1/A-2/1-1/1-2 스트레이트·크로스 트램라인`, 코드 없는 `스트레이트 트램라인` 단독형): 코드·방향·코스명 셋 다 서로 다른 형제와 구분하는 필수 식별자라 이미 최소 표현.
  - `스트레이트 슬라이스`(에디터가 최초 지적한 라벨), 그 외 "기술명+방향/목적지"만 남은 코스 라벨 다수(`클리어 → 내 백핸드 뒤`, `얕은 클리어 → 내 미드`, `가로채기 스매시 → 사이드` 등): 이미 기술명과 목적지뿐이라 더 뗄 부가정보가 없음.
  - `F-A/B/C`, `RF-1/2/3`(“일찍 읽고/늦게 잡아/균형 잃고” 조건형): 조건 자체가 그 갈래를 고르는 근거이자 학습 포인트라 축약하면 판단 기준이 사라짐.
  - 드라이브 랠리의 `가/나/다 …`, `내가 … 맞받음/잡음` 계열: 상대 공의 궤적을 묘사한 판단형 문장이라 축약하면 무엇을 고르는지 알 수 없어짐.
  - `클리어 되받기 → 내 백핸드 뒤`, `드라이브 되받기 → 내 몸`: "되받기"를 빼면 "내가 치는 기술"과 "상대가 받아치는 기술"이 헷갈릴 위험이 있어 보류(특히 "드라이브"는 방향성이 헷갈리기 쉬움).
  - `D-2/D-3 상대 깊은/짧은 리프트`: "상대"를 프롬프트 중복으로 보고 빼려 했으나, 형제 격인 다른 노드의 `리프트 상대 백핸드 뒤` 같은 손으로 쓴 코스명에도 "상대"가 실제로 쓰이고 있어(마크코스 자동 접미사와 다른, 손으로 쓴 지칭) 일괄 제거가 안전하지 않다고 재판단해 보류.
  - 이 51개는 여전히 12px·44px에서 "…"로 잘리지만, 위 축약 이전보다는 잘리는 지점이 뒤로 밀려(예: "네트샷/짧은 응수 → 여자 처리"는 원래 23자에서 14자로 줄어 "…" 앞에 더 많은 정보가 보임) 완전 무의미한 절단은 줄었다.
- 검증: 전체 클린 리빌드(v1.5 → apply_v16~v224.py). check-data 문제 0(노드 5177), check-recommended 문제 0, check-course-dir 871건 불일치 0, note-stats 형식 문제 0, regress.mjs 기존 허용 차이만(신규 0 — 이 스크립트는 base-nodes.json에 있는 단식 4개 주제만 비교 대상이라 이번에 고친 복식·혼복 라벨은 애초에 비교 범위 밖). vm으로 8곳 라벨 실제 반영 확인(예: "B-2 후위 쪽 (서버 파트너) — … (서버 파트너 백핸드)" — markCourses 접미사 정상 유지). 실측(12px, 실제 그리드 슬롯 95px): 고유 라벨 기준 잘림 73/225(32.4%, v2.23) → 51/225(22.7%). 헤드리스(390×844, 하이 서브·서브 측): 버튼 높이 [44,44,44,44]·[44,44,44] 균일 유지(리뷰어 지적 1 재확인), D-3a/B-2 축약 전/후 실측 overflow: true→false 확인. page errors 0. 파일 1,358,935B. 터널 = mobile/index.html.
- **정정(reviewId=wgr_84a17df3f6dca439f733fed3 수용): "전체 갈래 인스턴스 기준 1,365/4,265(32.0%, v2.23) → 1,011/4,265(23.7%, v2.24)"는 부정확한 값이었다.** 처음 첫 번째 슬롯(95px, 일반 배치·확률 배지 있음)의 잘림 여부를 전체 4265건에 그대로 곱했는데, 홀수 개 갈래의 마지막 `.wide` 버튼(그리드 두 칸 전체 = 280px 폭, 전체의 679건·15.9%)은 폭이 훨씬 넓어 같은 225개 라벨을 대입해도 **0개**가 넘친다(v2.23·v2.24 둘 다 동일하게 확인) — 이 차이를 반영 안 해 잘림 비율을 부풀렸다. `node.noProb` 노드(확률 배지 없음, 4건·0.09%)는 UI에서 혼복이 잠겨 있어(`SPORTS[].disabled`) 실제 폭 실측이 안 됐고, 비중이 0.09%라 일반 폭과 동일하다고 근사했다(오차 무시 가능한 수준).
  - 1차 재계산(일반 3,586건에 95px 하나만 적용): v2.23 = 1,242건(29.1%), v2.24 = 939건(22.0%)로 발표했으나, **이것도 "정확한 값"이라 단정한 것이 잘못**이었다(reviewId=wgr_c6337d0aa68419f2dbb8e006). `.pct`(비율 배지)는 "9%"·"37%"·"100%"·"불가" 등 자릿수에 따라 폭이 다르고(`flex: 0 0 auto`라 내용 크기만큼 차지), 그만큼 `.lbl`이 쓸 수 있는 공간도 달라진다 — 실측: 1자리(예 "9%") 103px, 2자리("37%") 95px, 3자리("100%") 88px, "불가" 99px, `.pct` 없음(`noProb`) 131px. 게다가 실제 %는 `branchProbs()`가 **현재 선택된 급수(tier)** 를 인자로 계산해 같은 노드도 급수마다 다른 숫자가 나오므로, "이 갈래는 항상 몇 자리"라고 정할 수 있는 고정값 자체가 없다 — 즉 애초에 급수와 무관한 단일 정답 비율은 존재하지 않는다.
  - 그래서 **단일 값 대신 범위**로 다시 냈다(같은 방법: 실제 진짜 DOM 버튼의 `.pct` 텍스트를 각 폭 케이스로 바꿔가며 225개 라벨 재대입 → 1자리/2자리/3자리/불가 각각의 core 잘림 집합을 DATA 전체(4,265건, wide 679건은 0건 확정)에 매칭):
    - v2.23: 1자리 기준 23.9% ~ 3자리 기준 29.6%(불가 기준 24.6%). 2자리(가장 흔할 걸로 보이는 경우) 중심값 29.1%.
    - v2.24: 1자리 기준 18.3% ~ 3자리 기준 27.2%(불가 기준 21.9%). 2자리 중심값 22.0%.
  - 어느 급수를 기준으로 봐도 v2.24가 v2.23보다 개선된 것은 확실하다(같은 노드·같은 급수 조건이면 %자릿수가 동일해 두 버전이 같은 폭 케이스로 비교되고, 그 안에서 라벨만 짧아졌으므로 잘림이 늘 수 없다) — 다만 "정확히 몇 %"라는 절대 수치는 급수에 따라 위 범위 안에서 움직인다는 것이 지금까지 확인한 사실의 한계다.
  - 고유 라벨 기준 수치(73/225, 51/225)는 애초에 wide·pct폭·급수와 무관한 "95px 슬롯 기준" 통계라 그대로 유효(하나의 고정 폭 기준으로 몇 개가 넘는지일 뿐, "전체 중 몇 %"라는 주장이 아님).

### v2.25 (2026-09-24): 홀수 개 갈래의 마지막 버튼 wide 제거 — 기본 크기로 통일 (사용자 "홀수 개 일때 일부러 크기를 늘리지 말고 기본크기로 해줘. 이미지 보면 선택 박스 크기가 너무 길어" + 스크린샷)
- v2.19에서 "빈 칸 방지" 목적으로 넣은, 홀수 개 갈래의 마지막 버튼을 그리드 두 칸 전체(`.wide`, 실측 280px)로 늘리던 규칙을 없앴다. 이제 홀수 개여도 마지막 버튼이 다른 버튼과 같은 1칸 폭(기본 크기)이고, 남는 칸은 그냥 빈 채로 둔다(CSS Grid 기본 배치).
- 적용: `renderBranches`에서 `btn.classList.add("wide")` 줄 삭제, `.branch.wide { grid-column: 1/-1; }` CSS 규칙 삭제(더 안 쓰이므로 정리). 데이터·다른 로직 변경 없음.
- 검증: 전체 클린 리빌드(v1.5 → apply_v16~v225.py). check-data 문제 0(노드 5177), check-recommended 문제 0, check-course-dir 871건 불일치 0, note-stats 형식 문제 0, regress.mjs 기존 허용 차이만(신규 0). 헤드리스(390×844): 사용자 스크린샷과 동일한 장면("1 상대 클리어 — 어디로?", 43%/29%/28%) 재현 — 버튼 3개 너비 [177,177,177]·높이 [44,44,44] 전부 동일(이전 v2.19~v2.24는 마지막 폭 362px로 늘어났었음), `.wide` 클래스 전부 false. 짝수 개(4개) 장면은 회귀 없이 [177,177,177,177] 그대로. page errors 0. 파일 1,358,834B. 터널 = mobile/index.html.
- 파이프라인: … → apply_v224.py → apply_v225.py.

### v2.26 (2026-09-24~25): 오프닝 후 메인메뉴(게임규칙/시작하기/경기일정) 신설 (사용자 "오프닝 후 화면 구성을 검토. 게임규칙, 시작하기, 경기일정 선택 화면이 첫화면. 게임규칙 클릭시 단식/복식 게임규칙을 아주 쉽게 이해할수 있는 화면 구성. 시작하기 버튼 클릭시, 기존 복식 단식 선택화면. 경기일정 버튼 클릭시, 2026년 국내 경기 일정 및 링크")
- **설계**: 오프닝 종료 후 곧장 종목/급수 선택(`#start`)으로 가던 흐름을 바꿔, 그 앞에 `#mainMenu`(게임규칙/시작하기/경기일정 3버튼)를 끼웠다. `#start`는 처음부터 `hidden`으로 시작해 "시작하기" 클릭 시에만 노출 — 오프닝(`box.remove()`로 사라짐) 아래에 이제 `#mainMenu`가 있는 구조라 오프닝 종료 로직 자체는 손대지 않았다. `#rules`(게임규칙)·`#schedule`(경기일정) 신설, 전부 `.start` 클래스(포스터 배경+반투명 카드)를 재사용해 기존 톤과 통일. 각 화면에 "메뉴로" 버튼으로 복귀.
- **경기일정 범위 확정(에디터 지적 2건 수용)**: 1차로 "대표 대회 몇 개만" 또는 "링크만"을 제안했는데, "낡을 위험"을 근거로 사용자가 명시한 범위("2026년 국내 경기 일정 및 링크")를 임의로 축소한 것이라 지적받음 → 실제 카테고리(국제투어/전국체전/협회 전국대회/동호인 대회)를 조사해 재질문 → 사용자가 "B결정, 서울 경기권 진행하는 경기"(전부 포함 + 지역 필터)로 확정. 이후 "축소하지 말고 정확히 확인해서 물어라"는 지적이 한 번 더 있었는데, 이번엔 실제 범위를 좁힌 게 아니라 사용자가 이미 정한 조건(전부+서울/경기) 그대로 데이터를 채운 것이라 재지적 대상은 아니었다.
- **경기일정 데이터 확보**: 코리아오픈(BWF World Tour, 11/3~11/8 여수)·코리아 마스터즈(BWF, 8/4~8/9 아산)·전국체육대회(제107회, 10/16~22 제주) 전부 지방 개최라 서울/경기 필터에서 자연히 빠짐(뺀 게 아니라 조건에 안 맞음 — 화면에 이 사실을 명시). 대한배드민턴협회 공식 사이트(koreabadminton.org)는 접속 실패(`ECONNREFUSED`)로 협회 주관 전국대회의 서울/경기 개최 여부를 확인 못 함(화면에 한계로 명시). badmintok.com이 대회 234건 전체를 schema.org JSON-LD(`SportsEvent`)로 구조화해 제공 — 처음엔 WebFetch(콘텐츠를 작은 모델이 요약)로 뽑았는데, curl로 원문을 직접 받아 대조하니 날짜 오류·존재하지 않는 항목이 발견돼 그 결과를 폐기하고, curl+Python으로 JSON-LD를 직접 파싱한 값만 채택했다. 확인일 2026-09-25 기준 그 날짜 이후 서울/경기 지역 30건.
- **게임규칙 콘텐츠**: 기존 출처(`bwf_laws`, BWF 공식 규칙) 기준으로 점수(21점 3세트, 듀스)·서브(높이·코트)·주요 폴트 등 공통 규칙과, 단식(좁은 폭·긴 코트·이동 부담)·복식(넓은 폭·짧은 서브 유효거리·파트너 로테이션·리시브 위치) 차이만 "아주 쉽게" 수준으로 추렸다. 서비스 높이 등 세부 수치는 다루지 않음(큰 틀만). 최근 규칙 개정 여부를 웹서치로 확인 — 특별한 변경 사항 없음.
- **적용**: `/tmp/patch/apply_v226.py` 신규(이전에 1.0.0 전환용으로 썼다가 사용자가 즉시 취소해 실제 파이프라인엔 포함된 적 없는 v226 번호를 재사용). CSS(메뉴 버튼 세로열, 시트형 카드 스크롤, 규칙 탭, 일정 목록), HTML(`#mainMenu`/`#rules`/`#schedule` 3섹션 + 기존 `#start`에 `hidden` 추가), JS(`RULES`/`SCHEDULE` 데이터, `renderRules`/`renderSchedule`, 메뉴 내비게이션 핸들러)만 추가 — 기존 로직·데이터는 안 건드림.
- **검증**: 전체 클린 리빌드(v1.5 베이스 → `apply_v16.py`~`apply_v225.py`, v29는 사용자가 "더 투명하게"를 요청 직후 철회해서 실제 파이프라인엔 원래도 없었음(`apply_v210.py` 자체 주석에 "v2.9는 철회" 명시) → `apply_v226.py`). `diff`로 v2.26 신규 추가분(버전 문자열, CSS/HTML/JS 추가 블록, `#start`의 `class` 속성 한 줄)을 뺀 나머지가 기존 배포된 v2.25와 완전히 동일함을 확인(회귀 없음). `check-data.mjs` 문제 0(노드 5177), `check-recommended.mjs` 문제 0, `check-course-dir.mjs` 871건 불일치 0, `note-stats.mjs` 형식 문제 0, `regress.mjs` 기존 허용 차이만(신규 0). 헤드리스(390×844): 오프닝 스킵 → `#mainMenu` 기본 노출·`#start` 비노출 확인 → 게임규칙(공통/단식/복식 탭 전환, 내용 정상 렌더) → 메뉴 복귀 → 경기일정(30건, 안내문에 출처·확인일·한계 정확히 표시) → 메뉴 복귀 → 시작하기(기존 종목/급수 화면 정상) → 단식 진입해 기존 게임 화면까지 정상 도달(회귀 없음) 전부 스크린샷 확인. 콘솔 에러 재현 안 됨(1차 실행 때 뜬 404는 재현 안 돼 favicon 등 무관한 것으로 판단). 파일 크기·정확한 줄 수는 커밋 시점 기준 `mobile/index.html` 참고.
- 파이프라인: … → apply_v225.py → apply_v226.py.

### v2.27 (2026-09-25): 리뷰어 지적 2건 반영 — 규칙 오류 수정 + 메뉴 복귀 연결 (reviewId=wgr_39ffe79eabbd26b8d7df0d34)
- **지적 1(규칙 내용 오류)**: v2.26 게임규칙 화면의 서브 항목("허리 아래·라켓 머리는 손 아래")이 2018년 이전 구식 규정이었다. BWF 공식 규칙(Laws of Badminton, 26 April 2025 V5.0, Law 9.1.6, PDF 직접 확인)은 "the whole shuttle shall be below 1.15 metres from the surface of the court at the instant of being hit by the server's racket" — 신장과 무관한 고정 높이(1.15m) 기준이지 허리·손 위치 기준이 아니다. 반칙 항목("네트에 셔틀·몸·라켓이 닿으면 반칙")도 틀렸다 — Law 13.4.1은 "선수가 라켓·몸·옷으로 네트나 지지대를 건드리는 것"만 반칙이고, 셔틀 자체가 네트를 스치고 넘어가 상대 코트에 떨어지는 것은 Law 15.1(친 사람 쪽으로 떨어질 때만 "인플레이 아님")에도 해당 안 돼 정상 타구다 — 원래 문구는 이 둘을 뭉뚱그려 정상 타구까지 반칙으로 설명하고 있었다. 두 항목 모두 원문 조항 그대로 재확인해서 수정.
- **지적 2(메뉴 복귀 연결 누락)**: 완료 기준("각 화면에 메뉴로 돌아가는 버튼")과 달리 `#start`(종목·급수 선택)에는 복귀 버튼이 없었고, 게임 화면 진입 후에는 `#mainMenu`로 갈 방법 자체가 없었다(`#btnSettings`는 설정용이지 메뉴 버튼이 아님). `#start`에 "메뉴로" 버튼 추가, 게임 화면 헤더(`.hrow`)에 "메뉴" 버튼 추가 — 클릭하면 게임 화면 위로 `#mainMenu`가 뜬다. 게임 진행 상태는 안 끊긴다: 메뉴에서 다시 "시작하기"→"시작" 버튼을 눌러도 `applySettings()`의 기존 분기(`topic.sport === settings.sport`면 `loadTopic` 재호출 안 함)가 진행 중이던 노드를 그대로 유지한 채 다시 그린다.
- 적용: `/tmp/patch/apply_v227.py`. `RULES.common`의 서브·반칙 문구 2곳, `#start`에 `#btnStartBack` 버튼, 헤더에 `#btnHeaderMenu` 버튼, 두 버튼의 이벤트 핸들러(`#mainMenu` 노출) 추가 — 그 외 로직·데이터는 안 건드림.
- 검증: 전체 클린 리빌드(v1.5 → apply_v16~v227.py), `diff`로 v2.26 대비 의도한 5곳(헤더 버튼, start 버튼, 서브 문구, 반칙 문구, 핸들러 2개)만 바뀌고 나머지 완전 동일 확인. 검증 스위트 5종 전부 기존과 동일 통과. 헤드리스: 수정된 문구 2개 그대로 렌더 확인, `#start`·게임 헤더 양쪽에서 메뉴 버튼 존재·클릭 시 `#mainMenu` 복귀 확인, 게임 진행 중(2단계까지 진행) 헤더 메뉴→mainMenu→시작하기→시작 버튼을 눌러도 단계("1 상대 리턴 — 뒤 중앙에서")가 그대로 유지됨을 확인(회귀 없음). page errors 0. 부수 효과로 헤더에 버튼이 하나 늘어 제목이 "Mind Control : Badminton v…"로 좀 더 일찍 잘리는데(기존에도 있던 `text-overflow: ellipsis` 안전장치가 더 일찍 작동하는 것뿐), 기능에는 영향 없어 이번 수정 범위에서는 그대로 둠.
- 파이프라인: … → apply_v226.py → apply_v227.py.

### v2.28 (2026-09-25): 게임규칙 오류 2건 추가 수정 (reviewId=wgr_525ad6495506a73b4a3221db)
- v2.27 완료 처리 뒤 남아 있던 오류를 리뷰어가 추가로 짚었다 — "화면에 잘 뜨는지" 검증과 "규칙이 맞는지" 검증은 다르다는 v2.27 교훈이 이번에도 반복됨.
- **지적 1(단식 서브 유효 구역)**: "서브도 랠리도 앞뒤 끝선까지 전부 유효"라고 써서, 짧은서비스라인 앞(네트 쪽)까지 서브가 유효한 것처럼 읽혔다. Diagram A(코트 도면)를 다시 보면 서비스 코트는 항상 짧은서비스라인부터 그 뒤 경계선까지의 구역이다 — 서브는 반드시 짧은서비스라인을 넘겨야 하고, 단식은 뒤쪽 경계가 복식보다 깊을(끝선까지, 복식처럼 롱서비스라인으로 안 당겨짐) 뿐이다. "서브"와 "랠리"를 뭉뚱그려 하나로 쓴 게 원인 — 랠리는 앞뒤 끝선까지 맞지만 서브는 다르다.
- **지적 2(복식 파트너 로테이션)**: "서브 넣은 사람이 반대 코트로 이동, 파트너는 자리 유지"가 서비스 코트 '배정'과 실제 서 있는 '위치'를 혼동시켰다. Law 9.5는 "서브 딜리버리 중 파트너는 자기 코트 안 어디든 설 수 있다(상대 시야만 안 가리면)"고 명시하는데, "파트너는 자리 유지"는 이와 반대로 읽힌다. 실제 규칙(Law 11.1.5)의 핵심은 "좌/우 서비스 코트 배정이 팀당 두 선수에게 고정돼 있다가, 그 팀이 서브로 득점했을 때만 서로 바뀐다"는 것 — 사람이 물리적으로 "이동해야 한다"는 동선 규정이 아니라 다음 서브 시점의 배정 규칙이다.
- 적용: `/tmp/patch/apply_v228.py`. `RULES.singles`의 "코트 길이"와 `RULES.doubles`의 "파트너 로테이션" 문구 2곳만 수정 — 그 외는 안 건드림.
- 검증: 전체 클린 리빌드(v1.5 → apply_v16~v228.py), `diff`로 v2.27 대비 의도한 2곳(문구 2개)만 바뀌고 나머지 완전 동일 확인. 검증 스위트 5종 전부 기존과 동일 통과. 헤드리스로 단식·복식 탭 렌더 결과 수정 문구 그대로 확인. page errors 0.
- 파이프라인: … → apply_v227.py → apply_v228.py.

### v2.29 (2026-09-25): 경기일정 캘린더화 + 게임규칙 개편 (사용자 "경기일정 캘린더 형식으로 변경 / 게임규칙 영역 사이즈 동일하게 맞춰 / 공통부분에서 점수 부분은 21점,25점 차이 설명 / 듀스 설명 / 서브에서 '…1.15m…' 문구 삭제 / '아주 쉽게 보는 핵심만…' 삭제 / 단식·복식 코트폭·코트길이는 코트 그림으로 설명, 체력 부담 삭제 / 내용이 너무 길어지면 페이징 처리")
- **경기일정 캘린더화**: 리스트(월별 헤더+항목 나열)를 월 그리드 캘린더로 전면 교체. `parseSchedRange`(문자열 "10.03~10.04" → Date 범위, 연도 2026 고정)·`schedEventsOn`(그 날짜의 대회 목록)·`renderCalGrid`(7열 그리드, 대회 있는 날은 `has-event`로 강조)·`renderCalDayList`(날짜 클릭 시 그 날 대회 카드 표시) 신규. `calPrev`/`calNext` 버튼으로 월 이동 — 이게 "내용이 길어지면 페이징" 요청의 경기일정 쪽 답이다(달 단위로 자연스럽게 나뉨). 기존 30건 데이터(SCHEDULE 배열)는 그대로, 표시 방식만 바뀜.
- **카드 크기 통일**: `.sheet-card`를 `max-height: 78vh`(내용 길이에 따라 카드 높이가 변함) → `height: 78vh`(고정)로. 게임규칙 공통/단식/복식 3탭 전환 시 내용 길이가 서로 달라도 카드 높이가 같아진다.
- **공통 규칙 재구성**: "점수" 항목에서 듀스 설명을 분리해 별도 "듀스" 항목으로(20:20 2점 차, 29:29면 30점 선취). "서브" 항목에서 "셔틀 전체가…1.15m…" 문구를 사용자 지시로 삭제(BWF Law 9.1.6 자체 서술이지만 이번엔 화면에서 뺐다 — 서브가 대각선으로 들어간다는 것만 남김). "21점, 25점 차이" 설명은 BWF 공식 규칙(Laws of Badminton, 26 April 2025 V5.0) 전체 14페이지를 이번에도 직접 재확인했으나 25점 관련 규정이 없다(21점 3세트, 20:20 듀스, 29:29면 30점이 전부) — 근거를 못 찾아 이번 패치엔 넣지 않고 사용자에게 직접 질문함(답변 대기).
- **안내문 삭제**: `#rules` 상단의 "아주 쉽게 보는 핵심만 — 자세한 규정은 BWF 공식 규칙 참고" 문구 삭제.
- **코트 그림화(단식/복식)**: `drawRuleCourt(mode)` 신규 — 게임 화면이 이미 쓰던 코트 좌표 상수 `C`(mobile/index.html:3167)와 SVG 헬퍼 `el()`을 그대로 재사용해 규칙 화면에도 코트를 그린다. 단식은 안쪽 사이드라인 밖(바깥 띠)이 아웃, 복식은 롱서비스라인 뒤(서브 시에만 아웃)를 음영으로 표시 — `RULES.singles`의 "코트 폭"·"코트 길이" 텍스트, `RULES.doubles`의 "코트 폭"·"서브 길이" 텍스트를 이 그림으로 대체(제거). 단식의 "체력 부담" 항목은 사용자 지시로 삭제.
- **버그(같은 세션에서 발견·수정)**: 최초 구현한 `drawRuleCourt`의 아웃 구역 음영이 `fill:"#0b1f12"`(짙은 녹색) `fill-opacity:"0.45"`로, 코트 배경(`#3f8f5a`, 중간 녹색)과 같은 색 계열이라 명도 차이만 있고 색상(hue) 차이가 없어 스크린샷에서 육안으로 거의 안 보였다(크롭 확대해도 마찬가지) — 게다가 단식 음영 폭 자체가 좁아(halfW−singles=0.46, 전체 폭의 7.5%) 축소 렌더 시 더 묻혔다. 색을 빨간 계열(`#e2384d`, opacity 0.62)로 바꿔 배경과 색상 대비를 확보하고, 경계선(단식 사이드라인/복식 롱서비스라인)도 노란색(`#ffd23f`)+굵게 그려 이중으로 강조해서 해결.
- **페이징**: 캘린더는 월 이동(calPrev/calNext)으로 이미 해결. 게임규칙 3탭은 실측 결과 `.sheet-card`(78vh 고정) 안에서 `clientHeight === scrollHeight`(658px, 스크롤 불필요)로 확인돼 페이징이 필요한 만큼 길지 않다 — 추가 조치 없음.
- 적용: `/tmp/patch/apply_v229.py`. CSS(`.sheet-card` 고정 높이, `.rule-court-*`, `.cal-*`), HTML(`#rules` 안내문 삭제+코트 wrap 추가, `#schedule`을 리스트 마크업→캘린더 마크업), JS(`RULES` 데이터 재구성, `drawRuleCourt`/`renderRules` 갱신, `renderSchedule` 전면 교체+캘린더 함수 4개 신규)만 변경 — 그 외 로직·데이터는 안 건드림.
- 검증: 전체 클린 리빌드(v1.5 베이스 → apply_v16~v229.py). 검증 스위트 5종(check-data 문제 0·노드 5177, check-recommended 문제 0, check-course-dir 871건 불일치 0, note-stats 형식 문제 0, regress.mjs 기존 허용 차이만·신규 0) 전부 기존과 동일 통과. 헤드리스(390×844): 공통/단식/복식 3탭 카드 높이 전부 658px 동일 확인, 단식 코트 wrap 표시+텍스트 항목 빈 배열 확인, 공통 항목 제목이 ["점수","듀스","서브","서브 코트","반칙(폴트)"]로 분리 확인, 안내문 삭제 확인, 캘린더 10월 라벨+대회 있는 날짜 강조+클릭 시 목록 표시+다음달 이동 확인, 기존 게임 진행 회귀 없음. 코트 음영 수정 후 크롭 스크린샷(단식·복식 둘 다)으로 빨간 아웃 구역+노란 경계선이 육안으로 명확히 구분됨을 확인, 카드 전체 스크린샷(공통/단식/복식)으로 레이아웃·캡션 문구 정상 확인. page errors 0.
- 파이프라인: … → apply_v228.py → apply_v229.py.

### v2.30 (2026-09-25): 21점/25점 차이 반영 + 캘린더 버튼 색 수정
- **21점/25점 차이(v2.29에서 근거 못 찾아 보류했던 항목)**: 사용자가 실제 대회 요강(2026 고양오픈 국제 배드민턴 페스티벌, 고양특례시배드민턴협회, 첨부 이미지 5장)을 근거로 제시 — "경기방법 1. 랠리포인트 25점(Maximum 25점) 경기로 실시함을 원칙으로 하되, 경우에 따라 집행부에서 변경할 수 있음", "2. 심판 및 경기에 관한 사항은 BWF 규정, BKA 규정 및 본 협회 규정에 따름". 즉 25점은 BWF 국제 표준이 아니라 국내 동호인 대회(시·군 협회 주관, 연령대·급수별 리그)에서 시간 단축을 위해 쓰는 자체 운영 규정 — 요강에 듀스 세부 규정(24:24 이후 처리 등)은 명시돼 있지 않아 과장하지 않고 "대회 요강마다 다를 수 있다"로만 서술했다. 이 시뮬레이터의 실제 진행 로직(21점 기준)은 이번 수정 범위 밖 — RULES.common의 "점수" 텍스트만 국제 표준(21점 3세트)과 국내 동호인 대회 방식(25점 단판) 둘 다 언급하도록 수정.
- **캘린더 버튼 색(사용자 "경기일정에서 버튼색 변경해줘 흰색이라 안보여 — 이전달, 다음달")**: v2.29에서 `#calPrev`/`#calNext`에 헤더용 `.mini`(흰 글자 + 흰 반투명 테두리, 투명 배경 — 네이비 배경인 `.hrow` 전용 스타일)를 그대로 갖다 써서, 밝은 카드(`.sheet-card`) 배경 위에서 거의 안 보였다. 카드 안 다른 버튼(`.sheet-back`="메뉴로")과 같은 톤(남색 테두리·남색 글자)의 `.cal-nav` 보조 클래스를 추가해 `.mini`와 같이 적용 — 레이아웃(padding·radius·flex)은 `.mini` 그대로, 색만 override.
- 적용: `/tmp/patch/apply_v230.py`. CSS 1줄(`.cal-nav`) 추가, HTML 2곳(`class="mini"` → `class="mini cal-nav"`), JS "점수" 항목 텍스트 1곳만 수정 — 그 외는 안 건드림.
- 검증: 전체 클린 리빌드(v1.5 → apply_v16~v230.py, 3864줄). 검증 스위트 5종 전부 기존과 동일 통과. 헤드리스(390×844): `#calPrev` computed style `color: rgb(36,53,76)`(=var(--navy))·`borderColor: rgb(36,53,76)`·배경 투명 확인, 스크린샷으로 남색 테두리+남색 글자가 카드 배경 위에서 명확히 보임 확인. "점수" 항목 텍스트가 21점(국제)·25점(국내 동호인, 요강마다 다를 수 있음)을 모두 언급하도록 반영 확인. 공통 탭 카드 `scrollHeight 661 vs clientHeight 658`(3px 초과, `overflow-y:auto`로 스크롤 처리되어 잘림 없음 — 페이징까지는 불필요), 단식·복식 탭은 여전히 658=658. page errors 0.
- 파이프라인: … → apply_v229.py → apply_v230.py.

### v2.31 (2026-09-25): 혼복 활성화 (사용자 "혼복 버튼 활성화해줘")
- **조사**: `SPORTS` 배열에 `{ id: "mixed", label: "혼복", disabled: true }`로 잠겨 있었다(주석: "미완성 종목 잠금"). 실제로 `mixed-serve-side`·`mixed-receive-side` 주제 데이터를 열어보니 노드 트리는 완결돼 있고(check-data.mjs 문제 0, 모든 경로가 end 도달), branches 구조(label/to/w/skill)도 다른 "level" 카테고리 주제와 동일해 급수(D~A조) 보정이 그대로 적용되는 상태였다 — 즉 noTier 없이 두는 현재 설계가 맞다. 잠긴 진짜 이유는 코드 주석에 이미 적혀 있었다(`nameOf` 함수 옆, "이름이 없는 주제(혼복, 잠금)는 id 그대로") — 실제로 `disabled`만 지우고 헤드리스로 확인해보니 코트 위 선수 라벨이 "W"·"M"·"R"·"RP" 내부 코드 그대로 노출됐다(추측이 아니라 실제 스크린샷으로 확인).
- **적용**: `mixed-serve-side`에 `names: { W: "여자", M: "남자", R: "리시버", RP: "리시버 파트너" }`, `mixed-receive-side`에 `names: { W: "여자", M: "남자", S: "서버", SP: "서버 파트너" }` 추가 — 다른 활성 주제(`serve-side`·`receive-side`)가 이미 쓰는 역할명 관행을 그대로 재사용(임의 작명 아님). `SPORTS`의 `mixed`에서 `disabled: true` 제거. names를 넣자 check-data.mjs의 "화면 글에 약자 남음" 검사가 mixed 주제도 대상에 포함시키면서 갈래 라벨 4곳에 남아있던 "(W)"·"(M)" 표기를 걸러냈다("여자(W) 서브 — 통상 위치" 등) — 이미 "여자"·"남자"라는 이름이 있어 중복인 괄호 표기만 제거.
- **검증**: 전체 클린 리빌드(v1.5 → apply_v16~v231.py, 3866줄). check-data.mjs "주제 12(이름표 10)" → "주제 12(이름표 12)", 문제 0. 나머지 4종(check-recommended·check-course-dir·note-stats·regress) 전부 기존과 동일 통과. 헤드리스(390×844): 종목 선택 화면에 "혼복" 버튼 활성화 확인, 선택 시 급수(D~A조) UI 정상 노출, D조로 게임 진입 시 코트 라벨 ["상대","우리","여자","남자","리시버","파트너"](기존 level 주제와 동일하게 "OO 파트너"는 "파트너"로 축약 — 기존 관행과 일치, 버그 아님 확인), 첫 갈래("여자 서브"/"남자 서브") 라벨에 내부 코드 없음, 다음 노드(mw_serve)에서 확률 배지가 급수에 따라 실제로 계산됨(D조 초급: 40%/60%, skill 보정 반영) 확인. page errors 0(간헐적 404 콘솔 로그 1건은 재실행 시 재현 안 돼 v2.26 때와 같은 무관한 노이즈로 판단).
- 파이프라인: … → apply_v230.py → apply_v231.py.

### v2.32 (2026-09-25): 기술 배우기 화면 추가 — 셔틀 궤적 시각화 (사용자 "모든 기술에 대해서 먼저 학습이 필요하잖아? … 어떤 기술들이 있는지 이 기술들이 어떻게 셔틀콕이 날라 가는지를 표현 … 탑뷰 … 아니면 사이드뷰 옆에서 보는 시각으로")
- **범위**: DATA의 `shot.type`으로 실제 쓰이는 33종 전부를 덮는다. base 기술 16개 + 변형 16개 = 32종이고, 나머지 하나인 "미스(네트)"는 기술이 아니라 네트에 걸린 실패 상황이라 기술 목록에서 제외했다(누락이 아니라 성격이 다른 항목 — 스크립트로 엄격 대조해 이 하나만 남는 것을 확인).
- **왜 사이드뷰가 기본인가**: 기존 게임 화면은 탑뷰뿐이고 `playShot()`이 궤적을 직선(line)으로 그린 뒤 셔틀 원의 반지름(`r = 0.16 + 0.32*H*4*t*(1-t)`)으로 높이를 암시할 뿐이라, 클리어·드롭·스매시·드라이브·네트샷이 화면에서 전부 비슷한 직선으로 보인다. 이 기술들의 본질적 차이는 높이 프로파일이라 옆에서 본 단면이라야 구분된다. 탑뷰는 좌우 코스가 핵심인 기술(크로스 네트샷, 푸시 변형 등)을 위해 토글로 제공.
- **구현**: `#techs` 섹션 신설(메인메뉴 "기술 배우기" 버튼 → 그룹 탭 5개[서브/뒤 공격/전환/네트 앞/수비] → 기술 칩 → 궤적 SVG + 설명). `TECHS` 데이터에 기술마다 `from[x,높이]`·`to[x,높이]`·`apex`(최고점 높이)·`at`(최고점 위치 비율)·`ms`·`top`(탑뷰 코스)·`when`/`aim`/`risk`·`vars`(게임에 나오는 변형 이름)를 둔다. 2차 베지어 제어점을 `techCtrl()`로 역산해 지정한 지점(at, apex)을 정확히 지나게 하고, 기존 `tween()`으로 셔틀을 궤적 위에서 움직인다.
- **스케일 처리(시행착오 포함)**: 처음엔 사이드뷰 viewBox를 코트 전체(±6.7m)·높이 7.5m로 고정했는데, 킬·네트샷처럼 낮고 짧은 기술이 화면 구석에 작게 찍혀 궤적이 안 보였다. 기술별로 필요한 범위(`hNeed`, 궤적 x 범위)에 맞춰 viewBox를 조이되 **SVG 박스 종횡비(약 1.7:1)에 맞춰** 위아래 빈 공간이 생기지 않게 했다. 스케일이 기술마다 달라지므로 **1m 간격 높이 눈금**을 함께 그려 실제 높이를 읽을 수 있게 했다(클리어는 6m 눈금까지, 킬은 2m까지 — 눈금이 있어 스케일 차이가 오해를 만들지 않는다). 글자·선 두께는 viewBox 가로 폭에 비례(`fs = W/34`)시켜 어느 기술에서나 화면상 같은 크기로 보이게 했다. 중간에 글자를 `top/18` 기준으로 키웠다가 낮은 기술에서 글자가 화면을 덮고 눈금 라벨이 잘리는 문제가 생겨 되돌린 이력이 있다. 탑뷰는 세로로 긴 코트라 `.tech-svg.top { height: 270px }`로 확대.
- **정직성 표기**: 궤적 수치는 실측 물리값이 아니라 코칭 자료를 바탕으로 한 학습용 모식도 — 화면 하단에 그대로 명시했다.
- **검증**: 전체 클린 리빌드(v1.5 → apply_v16~v232.py, 4100줄). 검증 스위트 5종 전부 기존과 동일 통과(check-data 문제 0·노드 5177, check-recommended 문제 0, check-course-dir 871건 불일치 0, note-stats 형식문제 0, regress 신규차이 0). 커버리지 대조 스크립트로 DATA의 33종 중 "미스(네트)"만 미포함임을 확인. 헤드리스(390×844): 메뉴 버튼 4개(기술 배우기 포함), 그룹 탭 5개, 칩 전환, 궤적 path 생성, 설명(언제/노리는 것/위험/변형) 렌더, 사이드↔탑뷰 토글(viewBox 전환), 다시 보기 재생 확인. 기술 화면을 거친 뒤 기존 게임 진입도 정상("0 하이 서브 — 중앙 깊게") — 회귀 없음. page errors 0.
- 파이프라인: … → apply_v231.py → apply_v232.py.

### v2.33 (2026-09-25): 리뷰어 지적 2건 반영 — 궤적이 네트를 관통하던 문제 + 변형이 이름만 있던 문제 (reviewId=wgr_8dc00060db7ab44f66e0e6ed)
- **지적 1(네트 관통)**: v2.32의 궤적 계산식을 실제로 대입해 전수 검증하니 네트(x=0) 통과 높이가 **빠른 드롭 1.08m, 드롭 0.88m, 블록 0.80m**로 네트 1.524m보다 낮았다 — 그림상 셔틀이 네트를 뚫고 지나간다. 원인은 apex를 타점 근처(`at` 0.03~0.05)에 두고 착지점을 네트 바로 앞에 둔 탓에, 2차 베지어가 네트 위치에서 이미 많이 내려와 있었던 것. 학습용 모식도라도 정상 타구를 잘못 가르치는 그림이라 **구조 자체를 바꿨다**: 기술마다 "네트를 지날 때의 높이(`net`)"를 데이터로 명시하고, 궤적을 `from →(0, net)→ to` **두 구간의 2차 베지어**로 그린다. 네트 통과점이 곡선 위에 못 박히므로 어떤 값을 넣어도 관통이 구조적으로 불가능하고, 뒤 구간 제어점을 네트 높이에 두면 셔틀 특유의 "넘자마자 급락"이 자연스럽게 나온다(드롭·블록 궤적이 실제 모양에 더 가까워졌다). `apex`/`at` 파라미터는 `net`으로 대체돼 사라졌다.
- **지적 2(변형이 이름뿐)**: v2.32는 변형 16종을 문자열로 나열만 해서, 이름 대조로 "모든 기술을 덮었다"고 한 것이 실질을 충족하지 못했다. 변형마다 **base와 무엇이 다른지(`note`)와 달라지는 궤적 값**(`net`/`to`/`ms`/`top`)을 주고, 화면에 **변형 칩 줄**(기본 · 변형1 · 변형2…)을 넣어 눌러서 그 변형의 궤적을 직접 보도록 했다. 예: 드롭(소프트)은 더 느리고(ms 1100) 네트를 더 낮게(1.64m) 넘어 더 짧게 떨어지고, 킬/푸시는 덜 가파르고 더 멀리 가며, 푸시(뒤 코너)는 전위 머리 위를 넘겨야 해 통과 높이가 1.75m로 더 높다. 변형 선택 시 설명 맨 위에 "무엇이 다른가" 줄이 먼저 붙는다.
- **검증**: base 16 + 변형 16 = 32개 궤적 전부에 대해 네트 통과 높이를 다시 계산 — **관통 0건**(여유는 네트 앞 기술 +0.04~+0.09m, 올려 보내는 기술 +2.0~+4.7m로 기술 성격과 일치). 변형 중 설명(`note`) 없는 항목 0건. 검증 스위트 5종 전부 기존과 동일 통과(check-data 문제 0·노드 5177 등). 커버리지 대조는 여전히 "미스(네트)"(기술이 아닌 실패 상황) 하나만 미포함. 헤드리스: 변형 칩 줄 렌더·변형 전환 시 궤적과 설명이 함께 바뀌는 것 확인, 드롭·블록 스크린샷에서 궤적이 네트 위를 지나 넘어간 뒤 떨어지는 것 육안 확인. page errors 0.
- 파이프라인: … → apply_v232.py → apply_v233.py.

### v2.34 (2026-09-25): 옆에서·위에서를 한 화면에 동시 표시 + 궤적 싱크 (사용자 "옆에서 뒤에서를 나누지말고 같은 화면에 동시에 실행하면 좋겠어. 궤적 싱크 맞추고")
- **토글 제거**: v2.32/v2.33은 단일 SVG `#techSvg`를 전역 `techMode`("side"|"top")로 분기해 둘 중 하나만 그렸다. `#techSideSvg`(옆에서 — 높이)와 `#techTopSvg`(위에서 — 코스) 두 개로 나눠 `.tech-view` 안에 세로로 쌓고, `#techSide`/`#techTop` 버튼·`techMode` 전역·핸들러 2개·`.tech-vbtns` CSS를 전부 제거했다. `#techReplay`("다시 보기")만 남는다.
- **세로 배치를 고른 이유**: 위에서 본 그림의 viewBox는 7.8×15.1(가로/세로 0.52)이라 좌우로 놓으면 옆에서 본 그림의 폭이 절반(약 200px)으로 줄어 궤적이 작아진다. 세로로 쌓으면 옆에서 본 그림이 카드 전체 폭(322px)을 쓴다. 높이는 옆 150px·위 180px로 잡아, 두 그림 아래 설명(`#techBody`)이 스크롤 없이 화면에 걸치도록 했다(`.sheet-card`는 78vh 고정 + `overflow-y:auto`).
- **싱크의 정의 — "같은 순간에 네트를 지난다"**: 옆에서 본 궤적은 `cv.split`(네트까지의 가로 거리 비율)에서, 위에서 본 코스는 y=0에서 네트를 지나는데 이 두 값이 기술마다 다르다(드롭: split 0.813 vs cross 0.840). 같은 진행도 `u`를 양쪽에 그대로 넣으면 한쪽이 먼저 네트를 넘어 보인다. 그래서 위에서 본 진행도를 **구간별 선형 재매핑**한다 — `u ≤ split`이면 `0~cross`로, 그 뒤는 `cross~1`로. 코스가 네트를 지나지 않는 경우(cross가 0.02~0.98 밖)는 재매핑하지 않고 split을 그대로 쓴다. 셔틀 둘은 `tween(tech.ms)` **하나**가 같은 콜백에서 움직인다(애니메이션이 두 개면 프레임이 어긋난다).
- **함께 고친 것 1(궤적 꼭대기 잘림)**: 옆에서 본 그림의 세로 범위를 `hNeed`(필요한 최대 높이 + 0.5m)로 잡으면서 바닥 아래 여백(`floorGap` = H의 11%)을 빼지 않아, 리프트처럼 높이 올리는 기술의 궤적 꼭대기가 화면 위로 잘렸다(v2.32부터 있던 문제). `hBox = hNeed / 0.89`로 잡아 여백을 빼고도 필요한 높이가 들어가게 했다.
- **함께 고친 것 2(라벨 겹침)**: 맨 위 눈금 글자가 화면 밖으로 잘리던 것(눈금 루프 상한에 0.6m 여유), 타점이 네트에 가까운 기술(리프트·헤어핀 등)에서 "네트 1.52m" 라벨이 시작점 셔틀 위에 겹치던 것(`from.x > -2`이면 네트 오른쪽에 붙임)을 고쳤다.
- **viewBox 종횡비**: v2.32는 SVG 박스 비율을 1.7로 하드코딩했는데 박스 높이가 180→150px로 바뀌면 어긋난다. `getBoundingClientRect()`로 실제 박스 비율을 읽어 viewBox를 맞춘다(화면에 붙기 전이면 CSS 기본값 322/150로 대체). 이 때문에 `#btnGoTechs` 핸들러에서 `renderTechs()`를 `#techs` 표시 **뒤로** 옮겼다 — 숨김 상태에서는 박스 크기가 0이다.
- **안 건드린 것**: `TECHS`의 궤적 수치(from/net/to/top/ms), 기술 목록·변형·설명 문구, 게임 화면(`#start`)의 코트 렌더링.
- **검증**: 전체 클린 리빌드(v1.5 → apply_v16~v234.py, 4188줄). 검증 스위트 5종 전부 기존과 동일 통과(check-data 문제 0·노드 5177, check-recommended 문제 0, check-course-dir 871건 불일치 0, note-stats 형식문제 0, regress 신규차이 0). 싱크 수치 검증(`/tmp/patch/sync-check.mjs`): 기술 32종(변형 포함) 전부 `u = split`에서 옆 x = 0·위 y = 0 — **네트 통과 시점 불일치 0**, 재매핑이 없었다면 어긋났을 기술 16종. 진행도 역행 0. 헤드리스(390×844): `#techSideSvg`/`#techTopSvg` 둘 다 존재하고 `#techSide`/`#techTop`/`#techSvg`는 없음, 캡션 "옆에서 — 높이"/"위에서 — 코스" 확인. 재생 중 90ms 간격 10회 샘플에서 옆 x와 위 y의 부호가 **같은 샘플(index 5)에서 동시에 뒤집힘**(-0.08/0.13 → 0.34/-0.59) — 두 그림이 같은 순간에 네트를 지남을 실제 DOM 좌표로 확인. 기술 32종 전수로 옆에서 본 궤적 path의 getBBox가 viewBox 안에 들어가는지 검사 — **잘림 0건**. 스크린샷(서브/숏서브, 수비/리프트, 네트 앞/헤어핀)으로 두 그림이 함께 보이고 설명까지 화면에 걸치는 것 확인. page errors 0(콘솔 404 1건은 파비콘 — 페이지에 `favicon` 참조가 없고 서버에도 파일이 없어 브라우저가 자동 요청한 것, JS 오류 아님).
- 파이프라인: … → apply_v233.py → apply_v234.py.

### v2.35 (2026-09-25): 리뷰어 지적 반영 — 변형이 옆 그림과 위 그림에서 서로 다른 곳에 떨어지던 문제 (reviewId=wgr_408818c1d4332a93a8f5cb75)
- **지적**: v2.34는 네트 통과 시점만 맞췄는데, "리프트(짧음)"은 옆 그림에서 네트 너머 4.2m에 떨어지지만 위 그림은 기본 리프트의 6m 착지점을 그대로 썼다. `techOf()`가 변형이 덮어쓰지 않은 `top`을 base에서 상속하기 때문. 두 시점이 서로 다른 착지 위치를 보여주면 궤적 싱크가 충족됐다고 볼 수 없다.
- **전수 확인**: 지적받은 1종이 아니라 **변형 16종 중 11종**이 같은 상태였다(스매시/슬라이스, 드롭(빠르게)·드롭(소프트), 드라이브(뜸)·드라이브(높게), 킬/푸시·킬/네트샷, 리프트(짧음)은 `to`만 덮어써서 위 그림이 안 따라오고, 네트샷(크로스)·푸시(크로스)·푸시(몸)은 반대로 `top`만 덮어써서 옆 그림이 안 따라왔다).
- **원인**: 착지점이 **두 군데에 따로** 적혀 있었다 — 옆 그림은 `to[x, 높이]`, 위 그림은 `top[.., .., .., 깊이]`. 같은 사실이 두 곳에 있으면 한쪽만 고쳐질 수 있다.
- **고친 방법(값을 하나씩 맞추는 대신 구조를 바꿨다)**: 코트 길이 방향 좌표를 `from[0]`/`to[0]` **한 군데에만** 둔다. 위에서 본 그림의 깊이는 옆 그림의 x를 그대로 뒤집은 값(`-x`)으로 매 프레임 계산하고, `top`에는 코트 좌우 좌표만 남긴다(`top: [출발 가로, 도착 가로]`, 4개 → 2개). 변형이 `to`를 덮어쓰면 위 그림의 착지점도 같이 움직이므로 **두 그림이 어긋나는 것 자체가 불가능**해진다.
- **부수 효과**: v2.34의 진행도 재매핑(`cross`/`split` 보정)이 필요 없어져 제거했다. 두 그림의 길이 방향 위치가 이제 같은 값이라 네트 통과 시점뿐 아니라 **비행 내내** 일치한다. 위 그림 셔틀의 좌우 위치만 그 진행 비율(`s = (x - from.x) / (to.x - from.x)`)로 잡는다.
- **기존 데이터와의 관계**: base 16종 중 10종은 이미 `top` 깊이 = `-x`였고, 나머지 6종도 차이가 0.05~0.40m(숏서브 `to.x` 1.6 vs `top` -2.0 등)로 손으로 다듬은 반올림 수준이었다. 이 변경은 데이터의 원래 의도를 코드로 강제하는 것이지 새 규약을 만든 것이 아니다. 함께 `TECHS` 설명 주석의 `apex`/`at`(v2.33에서 `net`으로 대체돼 이미 존재하지 않는 값) 서술도 고쳤다.
- **검증**: 전체 클린 리빌드(v1.5 → apply_v16~v235.py, 4183줄). 검증 스위트 5종 전부 기존과 동일 통과. 오프라인: 기술 32종(변형 포함) 전부 `top` 길이 2, 착지·깊이 불일치 0, 옆 그림 x 역행 0, 좌우 진행 비율 범위 이탈 0. 헤드리스 전수(32종): 재생 종료 시 **옆 착지 x + 위 착지 깊이 = 0**인 것 32/32, 불일치 0. 리뷰어가 짚은 사례 실측 — 리프트 기본 `옆 6 / 위 -6`, 리프트(짧음) `옆 4.2 / 위 -4.2`(이전에는 위가 -6이었다). 스크린샷으로 짧은 리프트의 위 그림 코스가 끝선 앞에서 끝나는 것 확인. page errors 0.
- 파이프라인: … → apply_v234.py → apply_v235.py.

### v2.36 (2026-09-25): 미터 수치 표시 제거 + 궤적 모양 수정 (사용자 "몇미터인지 표시는 지우자 / 포물선이 각도가 맞아? 자연스럽지가 않네")
- **미터 수치 제거**: 옆에서 본 그림의 1m 눈금 글자("1m","2m"…)와 "네트 1.52m" 라벨을 뺐다. 1m 간격 가로선과 네트 그림 자체는 남겨 높이 감각만 준다(수치는 적지 않는다). v2.32~v2.35에서 눈금 글자를 넣고, 잘림·겹침을 두 번 손보던 코드도 함께 사라졌다.
- **측정한 것(화면 곡선)**: "각도가 자연스럽지 않다"는 지적을 받고, 화면에 구현된 궤적식을 그대로 계산해 32종을 전수로 쟀다. **올려 보내는 기술 22종은 정점이 전부 네트(x=0)에 못 박혀 있었고** 그 지점 기울기가 0이었다 — 리프트는 정점이 비행의 **15%** 지점, 롱서브 25%, 클리어 44%. **내려치는 기술 10종은 네트를 넘은 직후 기울기가 0**이라 스매시가 네트 위에서 잠깐 수평이 됐다가 떨어졌다. 여기까지는 코드에서 나온 수치다.
- **판단한 것(어떤 모양이어야 하는가)**: 셔틀은 공기저항이 커서 올려 친 타구가 길게 올라갔다가 짧고 가파르게 떨어지는 것으로 코칭 자료에서 다루어진다. 정점이 비행 앞쪽 15%에 오고 남은 85%를 완만히 내려오는 기존 모양은 그와 반대다 — 이 대비가 수정의 근거다. 다만 **실제 비행을 계측해 비교한 것은 아니고**, 아래에서 새로 정하는 정점 위치·높이도 실측이 아니라 학습용으로 정한 값이다(화면 하단 "실측값이 아니라 … 학습용 모식도" 문구 그대로). `check-tech-curve.mjs`의 "정점 위치 20~90%" 기준도 물리 기준이 아니라 이 모식도에서 정한 범위다.
- **원인**: v2.33에서 네트 관통을 막으려고 네트 통과점을 곡선 위에 못 박으면서 **앞뒤 제어점 y를 둘 다 네트 높이(-net)에 뒀다**. 관통은 구조적으로 막혔지만, 그 대가로 네트에서 접선이 항상 수평이 됐다.
- **새 모델(타구 성격에 따라 둘로 나눔)**:
  - **올려 보내는 타구**: 정점을 데이터로 준다 — `apex: [타점→착지 구간에서의 정점 위치 비율, 정점 높이 m]`. 타점 → 정점 → 착지 두 구간이고 정점에서 접선이 수평이라 정점이 곡선의 최고점이 된다. 정점을 네트 뒤에 두면 "길게 올라가 가파르게 떨어지는" 모양이 나온다. 네트 통과 높이는 이제 데이터가 아니라 **결과**다.
  - **내려치는 타구**: 네트 통과점(`net`)을 그대로 곡선에 못 박아 관통을 구조적으로 막되, **네트에서의 기울기를 0이 아니라 앞뒤 평균 기울기 사이 값**(`s1 + 0.35(s2−s1)`)으로 잡아 네트 위에서 눕지 않고 계속 가팔라지게 했다. 타점이 네트보다 높은 타구가 라켓을 떠나자마자 떠오르지 않도록 앞 제어점이 타점 위로 올라가지 않는 선까지만 늘린다(이 제한이 없으면 드롭·스매시가 출발에서 +11°로 살짝 뜬다).
  - `net`을 덮어쓰던 변형 4종(드라이브(뜸)·드라이브(높게)·푸시(뒤 코너)·리프트(짧음))은 같은 뜻의 정점 높이로 옮겼다.
- **관통 방지 장치 교체**: 올려 보내는 타구는 네트 통과가 더 이상 구조적으로 보장되지 않으므로, 검사를 코드로 남겼다 — **`web/scripts/check-tech-curve.mjs` 신규**. 빌드된 `mobile/index.html`에서 `TECHS`와 `techCurve`/`techAt`를 그대로 떼어 읽어(화면과 같은 식으로 검사) 32종 전수로 ① 네트 통과 높이 ≥ 1.524m ② 진행 방향 역행 없음 ③ 내려치는 타구가 네트에서 평평하지 않고 타점 뒤에 정점이 생기지 않음 ④ 올려 보내는 타구의 정점 위치 20~90% 를 본다. 문제가 있으면 종료 코드 1.
- **정직성**: 새 정점 값도 실측이 아니라 코칭 자료를 바탕으로 한 학습용 값이다 — 화면 하단 "궤적은 실측값이 아니라 … 학습용 모식도입니다" 문구 그대로.
- **검증**: 전체 클린 리빌드(v1.5 → apply_v16~v236.py, 4196줄). 기존 검증 스위트 5종 전부 동일 통과. 신규 `check-tech-curve.mjs`: **32종, 문제 0, 최소 네트 여유 0.06m(킬/네트샷)**. v2.35의 착지점 일치 검사도 계속 통과(불일치 0). 모양 수치 — 올려 보내는 기술 정점 위치 30~81%, 출발 각 7~63°·착지 각 −25~−81°로 **착지가 언제나 더 가파름**, 내려치는 기술은 출발 −3~−24°에서 착지 −31~−69°로 계속 가팔라짐, 네트에서 평평한 구간 0. 헤드리스: `#techSideSvg` 안에 남은 글자 **0개**(미터 표시 제거 확인), page errors 0. 스크린샷으로 리프트(길게 올라가 네트 뒤에서 정점 → 급강하), 클리어, 스매시(네트에서 눕지 않고 계속 하강), 드롭(네트 넘자마자 급강하) 모양 확인.
- 파이프라인: … → apply_v235.py → apply_v236.py.
